## Verdict: APPROVE（首轮，head `477c1a73`）—— **台账对得上，「⛔ 等人拍板」这一节是对的做法**

按纯文档 PR 的口径：只对**实质性错误**拦。逐条核实过，没有。

### 台账数字我逐格对过

| 声称 | 实测 |
|---|---|
| 7 个 Task「均已合入 `preview`」 | ✅ **7/7 全部 MERGED → preview**：#143 `08-04T18:23` · #144 `02:11` · #148 `04:26` · #145 `04:33` · #146 `04:34` · #149 `05:15` · #152 `05:20` |
| 汇总表 `DONE 7 / PR_OPEN 1 / READY 4 / BACKLOG 12` | ✅ 逐条 `### T` 标题统计 **12/7/1/4，逐格相符**，总数 24 |
| 「M1 = 8 个 Task，7 个已合、1 个在评审中」 | ✅ `### T1.*` 恰好 **8** 个 |
| FU-6 的 `src=PR#T1.1.2` → `PR#148`（我在 #142 提的） | ✅ 改了，现在 10 条 FU 的 `src` **全部是 `PR#nnn` 形式**，去重键可解析了 |
| FU-10 的 `src=PR#T1.5.1` → `PR#152` | ✅ 同上 |

### 「⛔ 等人拍板」这一节值得单独说

把**两件实现者不该自己拍的事**从散落的注释里提出来、放在一个显式小节里，并且各自写了选项和理由——这是对的。尤其第 1 条里那句：

> 开一个「未改写原文」的旁路…**这条不该由实现者自己拍**：那等于开一个口子把未过滤的模型输出送出来，哪怕只给评测用，也是在削 D-3 的一层防线。

**识别出「这个技术方案会削弱一条既定安全约束，所以它是决策不是实现」，并且停下来问**——这正是我这几轮反复在挑的那类问题的反面。FU-9 加的那句「**排成独立 Task 做，不在评审周期里改**」也是同一个判断（#145 那轮的教训：大改一臂制造了 15 条假绿）。

### 建议随手带走（都不挡）

1. **`preview` 领先 `main` 的数字，文中有两个而且都不对。**
   ```
   :27  「领先 main **11 个 commit**」
   :76  「**9 个 commit** 压在 preview 上」
   实测  git rev-list --count origin/main..origin/preview = 10
   ```
   我猜 `11` 是算上了本 PR 合入之后的、`9` 是 #152 合入之前的残留。既然这个数字要支撑一次生产发布的决策，建议**别写死数字**，改成「`preview` 从未合过 `main`，当前领先量见 `git rev-list --count origin/main..origin/preview`」——这个文件自己在 `:15` 就写着「**不在这里重述数字**，上一轮就是重述的那份先过期了」，这条正好撞上同一个坑。
   （R1a 抓到了这两个数字互相矛盾，方向是对的。）

2. **`:76` 那句话的意思反了。**
   > 9 个 commit 压在 `preview` 上，包括**每天都在漏钱的那个洞**（客户打句 "Hi" 烧四次 GPU，#148）

   `preview` 上压着的是**那个洞的修复**（#148 = T1.1.2 出图触发闸），洞在 `main` 上。照字面读会得出「preview 里有个漏钱的洞」——而这是这一整节唯一用来论证「该合」的句子，意思反了会把论据变成反对论据。改成「`main` 上还留着那个每天漏钱的洞（客户打句 "Hi" 烧四次 GPU），修复在 `preview` 的 #148 里」。

3. **新的 `### 1. T1.5.1 的新 prompt 怎么验` 和它下面既有的 `## ⚠️ T1.5.1 的 live 基线：没有跑` 是同一件事的两份。** 两处都写了 A/B 两个选项、都写了 FU-5 那条「A4 在 live 下本来就瞎」的附带说明。这个文件自己反复论证「重述的那份总是先过期」——建议把既有那节缩成一句指针（「详见上面『⛔ 等人拍板』§1」），或者反过来，把新节缩成指针。留两份，下次改一处就会分叉。

### 驳回

- **R1a「`每天都在漏钱的那个洞` 是未经证实的断言，应给证据或弱化措辞」** —— 方向指对了句子但理由不对：这个断言**有据可查**（`tasks.md` 的 T1.1.2 背景段落写着 `enterThread()` 无条件调 `showBoxDirections()`、`RENDER_AVAILABLE` 为真就发四个付费 batch），#148 的评审也实测过。真正的问题是这句话把**洞**和**修复**的位置说反了，已改列在上面第 2 条。

### Assumptions

- 在两个独立 worktree（`origin/preview` 与 `477c1a73`）里核对，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件。
- 每个 PR 的合并状态都用 `gh pr view --json state,mergedAt,baseRefName` 实查，不采信文档自述；`preview` 领先量用 `git rev-list --count` 实测；标签统计用 `grep -E '^### T[0-9]'` 逐条数，与汇总表逐格比对。
- 按纯文档 PR 的口径：上面三条都是**内部精度问题**（数字对不上、措辞反了、重述了一份），按这个仓库既定的宽松线属非阻断，作为建议一次给出。
- **R2/R3/R4 未跑** —— 纯台账同步，无争议项，每条结论都有我自己实跑的命令输出。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons（`$pr` v4，**2 轮 + 机械验证**，首轮 head `477c1a73`）：DeepSeek R1a+R1b
（`deepseek-v4-flash`；2 条，其中 1 条是本轮首次独立抓到的真矛盾）→ Sonnet 机械验证（7 个 PR 的
合并状态逐个实查、`git rev-list --count` 实测领先量、24 条标签与汇总表逐格对账、M1 的 8 个 Task 计数、
10 条 FU 的 `src` 字段格式、新旧两节的重复度）→ **Opus R2 未跑（纯台账，无争议项）；Codex R3 未跑；R4 未跑**。*

### 自评

- **DeepSeek v4-flash 本轮：3/5 —— 它第一次独立抓到了一条真矛盾。** 第 1 条「`9 个 commit` vs 上面的 `11 个 commit`」**完全成立**，是这个 PR 里最实在的一处问题（虽然它没去实测正确值是 10）。第 2 条指对了句子、理由错了（说是「未经证实」，实际是「位置说反了」）。对一个 31 行的纯文档 diff，这个表现是这几轮里最好的一次之一。
- **下次怎么榨出更多信号**：它已经会做「同一份文档内两处数字对不上」的交叉比对了，缺的是**和仓库真实状态**比对。下次对台账类 diff，把 `gh pr list --state merged --json number,mergedAt` 和 `git rev-list --count` 的输出连同 diff 一起喂进去，并要求「逐条把文档里的每个数字/状态与给定的真实数据对照，标注一致/不一致」——它内部一致性已经能查了，外部一致性只差数据。
