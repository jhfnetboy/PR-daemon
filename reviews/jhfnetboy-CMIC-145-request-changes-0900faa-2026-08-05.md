## Verdict: REQUEST_CHANGES（增量复审，head `0900faa2`，round 5）—— **回归方向反过来了：这轮用假红换来了假绿**

**上一轮我拦的 8 条 A4 误红，全部真修，而且这次是【钉住】而不只是修掉。**

```
"The minimum size is 2mm."   "Minimum weight 200g board."   "12 months warranty"
"We did 2 weeks of prototyping first."   「我给你三天。」「做了三个月。」「分成三块。」「三个月的质保」
                                    8/8 在 3c07990 全红 → 在 0900faa 全放
```
`golden.json` 66 条**全绿**，而且上轮那 8 条**每条都有具名用例**（`a4-cn-three-days-not-lead-time` / `a4-weeks-warranty-no-of` / `a1-advice-then-confirm-cjk` …）。这是这几轮里第一次把回归钉死。变异 35 个杀掉 24（69%），**所有针对 A4 重写臂的删除/回退变异全部被杀**——新用例钉的是新逻辑，不只是我报的那 8 个字符串。

`ci-covers-check.test.ts` 也真收了：我实测 `if: false` / `continue-on-error: true` / `run: echo TODO` / 整步骤删掉 / 注释掉的步骤 / 只在步骤名里提——**全部抓住**。上轮我说「假阴性只是挪了位置」，这轮不是了。

---

**但修法是「删掉或收窄那一臂」，不是「加上排除项」。代价是：这轮在 A4 上买回来 15 条假绿——而 A4 是唯一 100% 硬闸、决定 `eval:chat --api` 的退出码。**

上轮我拦的是**假红**（吵、可见、会自己暴露）。这轮换成了**假绿**（静默，失败形态是一个真实报价发到客户手里）。

### 🔴 Blocking（4 条，全是本轮引入，全部新旧两版实测）

1. **[High] `checks.ts:319-320` — `months` 从无上下文那一臂里被删掉了，于是不带白名单交付词的月份交期整类逃逸。**
   ```
   3c07990 全拦 → 0900faa 全放：
     "It will take 3 months."          "Production takes 3 months."
     "We need 4 months for this run."  "Mass production window is 3 months from PO."
     "Bulk order timeline: 3 months."  "Ships in 3 months."
     "It ships in 3 months."           "Shipment is 3 months out."
   ```
   后三条还叠加了第二个原因：`\bship(?:ping|ped)?\b` **匹配不上 `ships` 和 `shipment`**，所以上下文那一臂也救不回来。
   （对照：`"Lead time 3 months."` 仍然拦得住——只有带白名单关键词的才活着。）

2. **[High] `checks.ts:319` — 小数确实开了一个 A4 假绿，但不是经 `sentences()`。**
   ```
   3c07990 拦 → 0900faa 放：  "Delivery in 2.5 months."
   仍然拦：                    "Delivery is 2.5 weeks."    "Delivery in 2 months."
   ```
   A4 **根本不调用切分器**。原因是新臂自己的间隔类 `[^.!?]{0,24}?` 跨不过 `2.5` 里的那个 `.`，而兜底臂又没有 `months`。`weeks` 那条活着只是因为 weeks 的兜底臂还在。

3. **[High] `checks.ts:328-330` — 中文交期改成必须带 `交期/工期/货期/出货/发货/交货/到货/交付` 白名单，而这个白名单漏掉了这行当自己的词。**
   ```
   3c07990 全拦 → 0900faa 全放：
     「大货三个月。」        「打样一周，大货三十天。」
     「生产周期三个月。」    「要三十天左右。」
   ```
   `大货` = 大货生产、`生产周期` = production cycle，这四条全是**真实的交期承诺**。杀掉「我给你三天。」是对的；用关键词白名单去杀不对——白名单只能覆盖你想到的说法。

4. **[High] `checks.ts:269` — 为了救「分成三块。」删掉的 `|[。.!！?？]|$` 分支，把两条真价格一起带走了。**
   ```
   3c07990 拦 → 0900faa 放：  「大概三块五。」   「一个盒子三块。」
   仍然拦：                    「大概三块五一个」
   ```
   缺口正好是**「价格出现在句末」——口语里最自然的形式**。

**修法（都是把排除项放到它该在的地方，而不是删掉一整臂）**：
```js
// :320 —— 恢复 months 到无上下文臂，排除项加在后面；顺带补小数和 ships/shipment
/\b\d+(?:\.\d+)?\s*(?:-\s*\d+\s*)?(?:working\s+|business\s+)?(?:days?|weeks?|months?)\b(?!\s+of\b)(?!\s+(?:warranty|prototyping|testing|design|development))/
// 上下文臂：\bship(?:ping|ped|s|ment)?\b
// :328 —— 中文交付词表补 大货/生产周期/打样/周期;「N天/周/月左右」本身即交期,不要求跟交付动词
// :269 —— 恢复句末分支,改成排除量词动词:  (?<!分成|切成|裁成)${CN_NUM}+\s?块…
```

### 非阻塞

- **[High] `scripts/ci-covers-check.test.ts:130` —— `viaCheck` 重写把一条死分支变成了【一行注释就能关掉整个守卫】的全局开关。** 我实测：
  ```
  删掉 ci.yml 里的 pnpm regress 一步 + 随便一行  run: pnpm -v # remember: pnpm check covers this
      → 守卫 rc=0，全绿
  同样删掉、不加那行注释
      → 守卫 rc=1，抓住
  ```
  `ciRunCommands` **刻意保留** `#` 注释和 echo 字符串（为了抓假引用），而 `viaCheck` 现在在这份文本上跑并且短路掉**每一条**逐步骤断言。新加的 `isFakeMention` 过滤器保护了 `direct`，唯独没保护这条能把它们全部禁用的路径。
  **修法**：`viaCheck` 也过一遍 `isFakeMention`，并在判定前剥掉行尾 `#` 注释。
  （我把它列为非阻塞只因为它是**测试基础设施**不是被测逻辑；但按后果它其实比上面四条更值得先修——一个能被注释悄悄关掉的守卫，正是它自己 docstring 在讲的那件事。）
- **[Med] `ci-covers-check.test.ts:89-90` —— 假阴性又挪了一次。** 两个检测器都锚 `\s*$`，所以**行尾一句注释**就能绕过——而那恰恰是救火的人会写的东西。我实测**绕过**：`if: false  # 临时救火,回头恢复`、`continue-on-error: true  # 临时`、`if: 'false'`、`if: github.event_name == 'never'`、`run: pnpm eval:chat || true`。实测**抓住**：裸 `if: false`、`if: ${{ false }}`、裸 `continue-on-error: true`、`echo "TODO: …"`、注释掉的步骤、只在名字里提、整步骤删掉。
  修法：`\s*$` → `\s*(?:#.*)?$`；接受带引号的 `'false'`/`"false"`；对 `run:` 加 `\|\|\s*true` / `exit 0` 检测。另外这个扫描是**全文件**不是**按步骤**的，一个无关上传步骤上正当的 `continue-on-error: true` 会把守卫打红。
- **[Med] `checks.ts:232-234` —— 新的 A6 旁路让纯提问变绿。** 只要一个 advice 词出现在疑问词**左边**就放行：
  ```
  3c07990 抓到 → 0900faa 放绿：
    「你推荐哪个盒型？」「推荐哪个盒型？」「最适合你的是哪个盒型？」「更适合你的是哪种？」「先按什么方向走，你说？」
  仍抓到：「哪个最适合你？」   ← 只因为语序反过来了
  ```
  这五条全是 A6 存在要抓的**「把问题原样抛回去」**。根因是 `ADVICE_STRONG` 里有裸的 `推荐`/`更适合`/`先按`，而这些词天天出现在问句里。修法：要求 advice 词是**真谓词**而不只是位置靠左（复用 `hasAdvice(first)`），或者把这三个裸词从语序判定里去掉。
- **[Med] `checks.ts:306` —— 重写的 `minimum` 臂把 round-2 的 `\b`-vs-CJK 问题带回来了。** `个|只|件` 是**死分支**（`/个\b/.test("个")` 为 false），`boxes` 没有单数形。实测 `minimum 500 个` / `minimum 500 个。` / `The minimum is 500 box.` / `The minimum run is 500.` 全部从拦变放。变异「删掉那三个 CJK 分支」**存活**，独立印证了死代码。
- **[Med] `checks.ts:319` —— 新臂的间隔类 `[^.!?]` 漏了 CJK 句末符**，而其它每一臂用的都是 `[^。.!！?？]`，于是它会跨过 `。！？`。这个方向是**误红**：`Delivery is TBD。 We did 5 days of testing on the coating.` → 判 lead-time，而同一句话换成 ASCII 句号就干净。中英混排在这里是常态，而 A4 误红会挂 CI。
- **[Med] A1 只关掉了两张白名单的【交集】（我上轮那条的后续）。** `ADVICE`(:95-106) 和 `ADVICE_STRONG`(:82) 是两张发散的表：`:129` 的闸要求 `ADVICE`，`:138` 熬过疑问句要求 `ADVICE_STRONG`。`建议用`/`推荐用` 在 STRONG 里不在 ADVICE 里（到不了闸）→「建议用天地盒，行吗？」仍 false；`I suggest`/`I would go`/`更适合` 在 ADVICE 里不在 STRONG 里（在 `:138` 被压掉）。而 `I recommend …, what do you think?` /「我会选天地盒，行吗？」这轮确实从 false 翻成了 true。**修法：从一张表派生，别维护两张。**
- **[Med] A4 完全没有「裸数量下限」那一臂**（两版皆有，非本轮）：`最少 500 个` / `至少 500 个。` / `500 个以上才做。` / `一次要订 500 个。` / `低于 500 个不接。` / `We start at 500 pieces.` / `Min order quantity is 1000` 全部逃逸。
- **[Low] 两版皆有的其它缺口**：`Unit price 2.8`（不带货币词的英文单价）、`开模费八百`（中文数字价格）、中文折扣整类（`8 折`/`打 8 折`/`半价`/`买 500 送 50`）。
- **[Low] 变异存活里值得补的**：`S2`（完全不按 `.` 切分）和 `S3`（只要求左侧有数字）都存活——小数规则只在**一个方向**被钉住，一个把英文回复全部塌成单句的变异能通过全部 66 条，使 A2 的 `questionCount` 和 A6 的首句选择对英文实际无钉。`L9`（阿拉伯数字句末分支）、`L10`（`(?<!第)` 误红守卫）、`A6-6`（`length > 30` 改 300）、`A6-7`（SUBSTANCE 去掉 `\d`）、`S4`（换行切分）均无用例。

### 驳回

- **R1a「`checks.ts:46` 的 `s[i]!` 非空断言会在畸形输入上崩」** —— 双重证伪：结构上，解引用在 `i < s.length` 内部，且非空断言是**纯类型层**、零运行时效果；实测上，我喂了 12 种畸形输入（`''`/`'.'`/`'...'`/`'。'`/`'2.'`/`'.5'`/`'a.'`/`'..........'`/`' '`/`'🎁.🎁'`/`'é.é'`/含 NUL 字节）给 `hasAdvice` 和 `forbiddenNumbers`，**零抛出**。R1a 还预言了「测试会失败」——66 条全绿。
- **R1a「`s[i-1]`/`s[i+1]` 可能是 undefined」** —— `:62` 两处都有显式 `?? ''` 默认值，而 `/\d/.test('')` 为 false，正是该走的分支。
- **R1a「`QUESTION_WORD` 的 `\b` 在 CJK 前可能匹配不上」** —— 方向对（这个文件确实有 `\b`-vs-CJK 的老问题，见上面 `:306` 那条），但它指的那处不成立。

### 一条方法结论

**这轮的教训不是「又漏了一个」，而是循环反过来了。** 前四轮全是假红——吵、可见、会自己暴露。这轮把它们换成了 A4 上的假绿——静默，而且 A4 是唯一 100% 硬闸。我上面四条阻断项里的每一条，都出自同一个动作：**用「删掉/收窄这一臂」去修假红，而不是「加一条精准的排除项」**。

根因你自己在 `:43-52` 已经写对了——**「改切分器，别改守卫」**——但没把它用到 A4 上。A4 现在有 22 条手调正则、**三种不同的句界约定**（`[^.!?]` / `[^。.!！?？]` / `[^。.!！?？\d]`）、两张发散的 advice 白名单、以及 CJK 分支塞在 ASCII `\b` 锚点里（这是这个文件里第三次犯 `\b`-vs-CJK）。A4 需要的是**一次 normalize-then-classify**（数字 + 单位 + 上下文窗口），不是第 23 条臂。

### Assumptions

- 在两个独立 worktree（`3c07990` 与 `0900faa`）里对跑同一组探针，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件；`ci.yml` 的攻击实验都在 worktree 内做并已还原，两个 worktree `git status` 干净。
- **每条都标了 REGRESSION / PRE-EXISTING**，四条阻断项全部经新旧两版对跑确认是本轮引入。
- **一处我自己的更正**：我第一次测 A6 用了 `hasStackedRequest`，两版都是 false，差点判成「非回归」；A6 实际走 `startsWithQuestion`（`:387`），换对函数后是清清楚楚的 5 条本轮回归。
- 本 PR 对 `preview` 冲突（`package.json` 等）。注意 **#145 分支的 `check` 串里没有 `pnpm test:gate`**（它在 #144 合入前开的分支），合并时若 take-theirs 会把唯一护着 GPU 花费的断言从 `pnpm check` 里静默删掉——详见我在 #142 上留的那条备注。
- **R3(Codex PK) 未跑、R4 未跑** —— 四条阻断项都有我自己在新旧两版实跑的输出，`viaCheck` 毒化和 CI 守卫绕过也是我自己构造并实跑的。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons（`$pr` v4，**2 轮 + 机械验证**，increment `3c07990`→`0900faa`）：DeepSeek R1a+R1b
（`deepseek-v4-flash`；3 条实测全部证伪，含一条会跑一次就证伪的「测试会失败」预言）→ Sonnet 机械验证
（新旧两 worktree 对跑确认 8 条 A4 回归全修且该拦的没松、golden 66 条全绿、逐个构造 9 种 CI 禁用手法
测守卫、实测 `viaCheck` 一行注释即可全局毒化）→ Opus R2（独立评审，发现回归方向已反转：15 条真价格/
真交期/真 MOQ 从拦变放，全在 A4 这条 100% 硬闸上；并指出 A6 的 ADVICE_STRONG 旁路、两张 advice
白名单只关掉交集、`\b`-vs-CJK 第三次复发；35 个变异指出小数规则只单向钉住）→
**Codex R3 未跑；R4 未跑**。*

### 自评

- **DeepSeek v4-flash 本轮：1/5。** 三条 findings 全部证伪，其中主打的那条（`s[i]!` 会在畸形输入上崩）**结构上就不可能成立**——非空断言是纯类型层、零运行时效果——而它还附带预言「测试会失败」，跑一次就没了。它对着一个刚重写的、有 22 条正则的规则文件，输出的全是 TypeScript 语法层面的担忧，一条行为缺陷都没碰。
- **一个我自己的方法教训（比 flash 的分数更值得记）**：我这轮的探针只查了「上轮我拦的假红修好没有」和「该拦的有没有被修松」，**7 条**该拦的全绿就收工了。R2 用的是同一个方法但把语料放宽到**类**（months 的各种说法、中文行业交期词、句末价格），一下子挖出 15 条。**验回归不能只回放上一轮的字符串，要回放那一整类。** 下一轮我在这个文件上先建一张按「价格/MOQ/交期 × 中英 × 关键词在前/在后/无关键词」展开的矩阵语料，再跑两版对比。
