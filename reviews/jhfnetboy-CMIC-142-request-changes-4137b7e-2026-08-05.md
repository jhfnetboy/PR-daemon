## Verdict: REQUEST_CHANGES (incremental re-review, head `4137b7e`, round 4)

**上一轮两条阻断项都改到位了，而且改法比我建议的更好。**

1. **单位口径** —— `tasks.md:263` 的断言 ④ 现在是「**落库值恒等于费用表的返回值**，写入路径不许有任何乘除法。口径的唯一权威是 `spec.md` §4」。它**引用权威而不是复述数字** —— 这正是我上轮提的方法修复，比逐处同步耐久得多。全仓 grep：`美元/美分/5900/59` 的每一处现在都自洽，`59（美元）` 零残留。
2. **`no-real-pii.sh` 那条不可执行的指令** —— `spec.md:27`（D-7 硬约束行）现在**反过来**明写：⚠️ 但**不要**去「把表加进 PII 扫描」，并解释了脚本按 `git ls-files` 全仓扫、needle 从 `docs/data/` 自动提取，还引了它自己那 20 行「不用黑名单」的论证。三个 Task 的「涉及文件」也都把这个脚本去掉了。我逐行核对了脚本，这些描述**全部属实**。

上轮四条非阻塞也全修：`leads` 的 `address` 改成 `website` 并加了「`leads` 没有 address 列」的注；L2 触发条件表改成「⚠️ **不是**『必须四槽齐备』，明确指令压过一切」；`preview` 分支状态改成「领先 main 1 个 commit（`65c6927`）」（与实际一致）；T1.3.1 补上了验收断言 ⑤。台账算术也干净：24 Task / 14 Feature，`DONE 1 + PR_OPEN 3 + READY 4 + BACKLOG 16 = 24` 与逐条标签相符，依赖链三处一致，验收命令引用的非 `(新)` 文件全部存在，零悬空文档引用。

**但同一类矛盾又剩了两处 —— 而且这次落在全文唯一自称仲裁者的那一节。**

### 🔴 Blocking

1. **[High] `docs/design/agent-interaction-v2.md:607` — §8 决策表第 7 行仍然下达那条被 `spec.md:27` 明令禁止的指令。**
   ```
   :606（第 6 行，本轮已改）「⚠️ 不要去「把表加进 PII 扫描」—— 那个脚本明确不用黑名单，全仓自动扫」
   :607（第 7 行，未改）  「…进角色剥离矩阵 + `no-real-pii.sh`（§6.3）」
   ```
   紧挨着的两行给出相反指令；而 `:607` 引为出处的 §6.3（`:555`）**本轮也已改成**「PII 扫描不用管」。
   最要命的是 `:667` 声明：「**不一致时以 §8 决策表为准**…§8 决策表是所有轮次收敛后的唯一结果」—— 全文唯一被指定为仲裁者的地方，恰恰是没改的那处。照它开工的人会去加一份表名黑名单，复活脚本明令否掉的模式。

2. **[High] `docs/design/agent-interaction-v2.md:326` — `leads` 的正文段落同样没改**：「并且要进 `scripts/no-real-pii.sh` 的检查范围」。这是第 6 行的正文对应段，行改了、正文没改。T3.1.1 和 T2.3.1 都引用这份设计稿，所以这两处是实现者真会读到的，不是死文本。
   **修法**：`:607` 照 `:606` 重写；`:326` 删掉那半句，替换成「（PII 扫描全仓自动，无需登记 —— 见 `../agent/spec.md` §0）」。

### 非阻塞

- **[Med] `docs/agent/tasks.md:471-479` — 「建议起手顺序」仍把已 `DONE` 的 T1.1.1 列为序 1**，序 2（T1.2.2）和序 4（T1.4.1）是 `PR_OPEN`，5 行里 3 行过期。
  而**这一轮恰好把 `progress.md` 里那张一模一样的表删掉了**，理由是「重述的那份总是先过期」，并让读者去看 `tasks.md` —— 过期的那份正在那里。教科书式的 N-1。
  建议用同一副药：要么删掉，要么降级成一条**不会过期的规则**（「优先做依赖链的根 T3.1.1，其次省钱的 T1.1.2」），不要带每个 Task 的序号。
- **[Low] `progress.md:34` / `tasks.md:446` — FU-1（PR #147，OPEN）只出现在分支表里**：没有 Task 条目、不在 24 条状态汇总里、不在 PR 列表里、不在变更日志里。pilot 读台账时看不到 #147。要么补一行并调整计数，要么明写「follow-up 修复不进 M→F→T 台账」，让在途工作的视图完整。

### 驳回的 findings

- **R1a「断言 ⑤ 没有更新的测试命令」** —— 不成立。`origin/feat/T1.3.1-slot-defaults` 上的 `apps/web/test/resolve-slot.test.ts` 已经用**逐字相等**断言明示清单（`:77`/`:84`），也覆盖了非法 stated → `overrode`（`:25-35`）和 `knowledge` vs `default` 的区分（`:44-49`）。同一个文件，不需要新命令。
- **R1a「`spec.md:14` 引用 #142 没有链接」** —— 行号是 `:13`，而且它是 PR #142 内部的自引用。措辞小事，不是 finding。

### 一条观察

方法修复确实落地了，**但只落在 `docs/agent/`**。这一轮改了设计稿里 5 处 PII-扫描表述中的 3 处（`:140-143`、`:553-555`、`:606`），漏了 2 处（`:326`、`:607`）—— 正是这个 PR 自己的新前言所诊断的那个 N-1 形状。

前言开的药方（「引用而不复述」）用在了 `tasks.md → spec.md` 上，但设计稿仍然在 5 个独立位置各自复述 PII 政策，且没有一处指向 `spec.md`。**让 `agent-interaction-v2.md` 在 D-7/D-3 上像 `tasks.md` 那样改为引用 `spec.md`，才是关掉这一类而不是又关一个实例。**

另外，前言称赞的「以 §8 决策表为准」这个机制，这次恰恰是它失效了：§8 在设计稿**内部**是权威，于是一行过期的 §8 反而压过了已经改对的正文。**权威引用只有在「被引用的那处保持最新」时才有用** —— 建议明写：数据/PII 口径上 `spec.md` 高于 §8。

### Assumptions

- 在独立 worktree（head `4137b7e`）里 grep/读全部 `docs/agent/*.md` 与 `docs/design/*.md`，并对照真实的 `scripts/no-real-pii.sh`、`git log origin/main..origin/preview` 与 GitHub 上的 PR 状态，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件。
- 按纯文档 PR 的口径，只对**实质性错误**判阻断。上面两条会让实现者复活脚本明令否掉的黑名单模式，且两个 Task 都会读到，所以属实质性；「建议起手顺序」过期只影响排期建议、不影响写出来的代码，降级为非阻塞。
- **我上一轮的疏漏**：那次我 grep `no-real-pii` 时用了 `head -6`，把设计稿里这两处截掉了，所以上轮报的是「三个 Task 的涉及文件 + spec/roadmap」，漏了 design 的 2 处。这轮是 Opus 独立读出来的，我事后核实。
- **R3(Codex PK) 未跑** —— 本会话内它已两次零输出挂起被杀。**R4 未跑** —— R2 判定 2-round，两条阻断项
  都有我自己的 grep 输出。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons (`$pr` v4, **2-round + 机械验证**, incremental `1cd268f`→`4137b7e`): DeepSeek
R1a+R1b（`deepseek-v4-flash`;2 条 findings 均不成立）→ Sonnet 机械验证（逐条 grep 核实上轮 2 条阻断项
与 4 条非阻塞、对照 no-real-pii.sh 全文、`git log origin/main..origin/preview` 核对分支状态、全仓悬空
引用扫描）→ Opus R2（独立评审,发现设计稿里 PII-扫描指令还剩 2 处未改,其中一处正在全文自称仲裁者的
§8 决策表里；并指出「建议起手顺序」是本轮刚从 progress.md 删掉的那张表的副本）→
**Codex R3 挂起未跑；R4 未跑**。*
