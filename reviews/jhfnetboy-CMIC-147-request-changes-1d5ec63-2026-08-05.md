## Verdict: REQUEST_CHANGES（增量复审，head `1d5ec63`，round 4）—— **只剩一条，1 行，其余全部通过**

**上一轮四条阻断项全部真修，63/63 零回归。** 「按值的形状判、不按关键词邻近判」这个改法是对的：

| round-3 的问题 | 现在 |
|---|---|
| 无值的问句型 chip 被误伤 | ✅ `Quote 2 options`/`Price 2 colors`/`Quote 3 sizes`/`Price A5 size`/`Quote A4 box`/`Price v2 layout` **6/6 放行** |
| 关键词+值逃逸 | ✅ `Price is about 2.8`/`Our price will be 2.8`/`Price: around 2.8`/`Cost 2.8` **4/4 拦截** |
| 货币代码前置整类逃逸 | ✅ `USD 2.8`/`RMB 2.8`/`CNY 500`/`EUR 3` **4/4 拦截** |
| `tg` 不在联系方式表 | ✅ `Tg @seller`/`tg: seller01`/`Add on TG` **3/3 拦截** |
| 零回归 | ✅ 第一轮 9 条绕过仍全拦；11 条中英孪生的价格/MOQ/交期仍全拦；裸「微」6/6 仍放行；20 条合法 chip 20/20 仍放行 |

`package.json` 那条也修得比我建议的好：`rc=0` 初始化了，还加了逐文件的 `❌ FAILED:` 标记和汇总行，`exit $rc` 在最后一句所以短路不会吞掉失败码。

**变异测试：30 个子句只剩 1 个存活**（round 2 是 8/26，round 3 是 4/28）。上轮那些「互相掩护」的用例（`每件 8`、`交期 15`、noop-redact 的 `13800138000`）确实各自杀掉了它们该杀的变异。

**卡住的只有一条，而且是同一个漂移类别连续第三轮：值形状的修复只落在英文那根轴上。**

### 🔴 Blocking（1 条，1 行）

**[High] `apps/api/src/chat.ts:152` — 中文子句仍是原来的「关键词 + 数字邻近」，没跟着改成值形状。**
```js
/(?:单价|售价|报价|开模费|模具费)[^。.!！?？\d]{0,8}\d/
```
成对实测，**7/7 中文被拦，英文孪生全部放行**：
```
中 拦 ❌ 「报价 2 个方案」    英 放 ✅ "Quote 2 options"
中 拦 ❌ 「报价 3 个尺寸」    英 放 ✅ "Quote 3 sizes"
中 拦 ❌ 「报价 A5 尺寸」    英 放 ✅ "Price A5 size"
中 拦 ❌ 「单价 2 种」       英 放 ✅ "Price 2 colors"
中 拦 ❌ 「售价 3 档」       英 放 ✅ "Quote 3 options"
中 拦 ❌ 「每盒 12 个」      英 放 ✅ "12 per box"
中 拦 ❌ 「每盒 500 只」     英 放 ✅ "500 per box"
```
这正是 round-3 判阻断的那 6 个形状，只是换到了中文轴上 —— 而 `微信`/`起订` 就在拦截表里，说明中文才是主市场。`报价 2 个方案`、`每盒 12 个` 恰恰是这个 agent 最常给的方向，会重新打开文件头自己警告过的 `chips: []` 级联。

**修法（1 行，R2 验证过：113 条断言全绿，6 个中文形状全部转为放行，而 `单价 2.8`/`报价 2.8`/`售价 500元`/`开模费大概 800` 仍然拦住）**：
```js
new RegExp(String.raw`(?:单价|售价|报价|开模费|模具费)[^。.!！?？\d]{0,8}${MONEYISH}`)
```
顺带 `:159` 的 `/每(?:个|只|件|盒)\s?\d/` 建议一并处理 —— `:157` 的注释自己论证了「`500/box` 没有小数，那是每箱 500 个的数量档，不是价格」，然后把这句话的中文原型 `每盒 12 个` 拦掉了。两边取同一个口径就行。

### 非阻塞（建议后续）

- **[Med] `:167-170` MOQ_VALUE 两种语言最自然的说法都漏**：`Minimum 500 pcs` / `Min 500 pcs` / `500 pcs minimum` / `At least 500 pcs` / `最少 500 个` / `500 个起` / `500起订` / `最低 500` 全部逃逸。注意 `起订 500` 拦得住但 `500起订`（中文正常语序）不行 —— `起订…\d` 只匹配关键词**之后**的数字。本轮编辑过这个子句，所以顺手一起收了更好。
- **[Med] `:143` MONEYISH 少了千分位**：`Price around 2,800` / `Unit price 1,200` / `Quote: 12,000 total` / `Price starts at 2,500` 全部逃逸。直接加会撞上 `Quote 1,000 pcs`，需要带单位否定前瞻：`|\d{1,3}(?:,\d{3})+(?!\s*(?:pcs?|pieces?|units?|sets?|boxes?|个|只|件|盒))`。
- **[Low] `:170` 本轮新加的 `\bstarting\s+(?:at|from)\b[^\d]{0,6}\d`** 恰恰是这个 commit 要消灭的「关键词邻近」形状：`Starting from A4 size` / `Starting at 3 options` 现在会被拦。建议要求数量单位。
- **[Low] `:186,192`** — 裸 `tg` 进品牌表 + `[@\w-]{2,}`（原来是 `{3,}`）让任何独立的 `tg`/`ig`/`wx`/`qq` 加一个 2 字符 token 都不安全：`TG board`、`IG post mockup` 会被拦。为了 `Add on TG` 这是可接受的取舍，但值得加一句注释，免得第五轮有人盲目"修"它。
- **[Low] 唯一的变异存活**：`/(?:元|美元|人民币)\s?\d/` 是活的（`人民币 500` 只有它能拦），但没有断言钉住它，补一条即可。
- **[Low] 仍然逃逸的**：中文数字（`两块八`/`大概三块`/`单价两块八`）、混淆联系方式（`微 信`/`V:seller01`/`扫码加我`/`sales(at)example(dot)com`）、以及 `Signal`（品牌表里已经有 viber/zalo/line，独漏它）。

### 驳回

- **R1a「MONEYISH / CUR_CODE 太松，允许不带小数的整数」** —— 实测证伪：整数是被货币标记 gate 住的（`USD 2` 拦、`Price 2`/`Quote 500` 放），这正是值形状的设计语义。R1 把符号搞反了 —— MONEYISH 真正的缺陷是**太紧**（千分位，见上）。

### 一条方法建议

这个文件反复出问题的不是某一条正则，而是**修复总落在一根语言轴上、镜像子句被留下**。round 2、round 3、round 4 各出了一次一边倒的修复。

`chips.test.ts:100` 已经为**拦截**语料建了中英 `PAIRS` 表 —— 把**放行**语料也做成同样的成对表，这一类就再也过不去了。今天这条 finding 就是我把中英孪生并排跑才立刻显形的。

另外：变异覆盖已经接近满（29/30 钉住），剩余风险已经完全从「没测到的子句」转移到「没枚举到的说法」—— 词形闸门到顶了。如果 D-3/D-7 的泄漏不止是 best-effort，耐久的答案是拿 chips 去和确定性报价引擎自己的值做校验，而不是继续长正则表。

### Assumptions

- 在独立 worktree（head `1d5ec63`）里跑测试、变异与探针，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件。
- **R3(Codex PK) 未跑** —— 本会话内它已两次零输出挂起被杀。**R4 未跑** —— R2 独立判定 2-round，
  阻断项有我自己的中英成对实测输出。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons（`$pr` v4，**2 轮 + 机械验证**，increment `2c7306e`→`1d5ec63`）：DeepSeek R1a+R1b
（`deepseek-v4-flash`；1 条实测证伪且符号搞反）→ Sonnet 机械验证（63 条双向语料确认四条阻断项已修且零回归、
实测 `rc` 初始化与逐文件失败标记、中英成对探针暴露单侧修复）→ Opus R2（独立评审，定位到中文子句未跟改、
MOQ 两语言最自然说法均漏、MONEYISH 缺千分位，并用 30 个变异确认只剩 1 个存活）→
**Codex R3 挂起未跑；R4 未跑**。*
