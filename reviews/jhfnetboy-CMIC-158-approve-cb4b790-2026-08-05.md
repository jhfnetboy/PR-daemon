## Verdict: APPROVE（首轮，head `cb4b790d`）—— **台账数字对得上，一处遗留的旧句子建议顺手改**

按纯文档 PR 的口径：只对**实质性错误**拦。核实过，没有。

### 逐格核对

| 声称 | 实测 |
|---|---|
| 8 个 Task 全部 `DONE`，对应 8 个 PR | ✅ **#143 / #144 / #148 / #145 / #146 / #149 / #152 / #151 逐个 `gh pr view` —— 8/8 全部 MERGED** |
| 汇总表 `DONE 8 / PR_OPEN 0 / READY 4 / BACKLOG 12` | ✅ 逐条 `### T` 标签统计 **8/0/4/12**，总数 **24 = 实际 `### T` 标题数** |
| M1 的 8 个 Task 都是 `DONE` | ✅ `T1.1.1 / T1.1.2 / T1.2.1 / T1.2.2 / T1.3.1 / T1.3.2 / T1.4.1 / T1.5.1` 全部 `DONE` |
| 两条「等人拍板」结案 | ✅ 第 2 条已划掉并标「**已合并并发布（v1.2）**」；分支表也同步成「已于 2026-08-05 合入 `main` 并发布生产（tag `v1.2`，回滚点 `v1.1-prod-rollback`）」 |

**留了回滚点 `v1.1-prod-rollback` 这一条值得记一笔**——把「发布」和「怎么退回去」写在同一行，比只记发布 tag 有用得多。

### 建议随手带走

- **[Low] `tasks.md:471` 还是旧句子：「M1 = 8 个 Task，**7 个已合、1 个在评审中（T1.3.2）**。」** 而上面的表格和逐条标签都已经是 8/8 `DONE`、`PR_OPEN 0`，`#151` 也确实在 `05:59` 合了。同一页里两处对同一件事说法不同。改成「**8 个 Task 全部已合**」即可。
  （R1a 抓到了这条——**这是它连续第二次在台账类 PR 上独立发现真实的内部矛盾**，上一次是 #153 的「11 vs 9」。）
- **[Low] `progress.md:52` 说 `.pilot.yml` **尚未进 `main`**（在 `chore/pilot-bootstrap` 上、cherry-pick 到规划分支）。** 既然 `preview` 已经合进 `main` 并发布，这条值得复核一下现在还成不成立——如果那个 cherry-pick 随 `preview` 一起进去了，这句就过期了；如果确实还没进，那它现在是 `main` 上唯一一处「配置靠 cherry-pick 维持」的地方，值得单独排一个清理项。

### Assumptions

- 在两个独立 worktree（`origin/preview` 与 `cb4b790d`）里核对，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件。
- 每个 PR 的合并状态用 `gh pr view --json state` **逐个实查**，不采信文档自述；标签统计用 `grep -E '^### T[0-9]'` 逐条数并与汇总表逐格比对。
- 按纯文档 PR 口径：上面两条都是内部精度/待复核项，不构成实质性错误。
- **R2/R3/R4 未跑** —— 纯台账同步，无争议项。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons（`$pr` v4，**2 轮 + 机械验证**，首轮 head `cb4b790d`）：DeepSeek R1a
（`deepseek-v4-flash`；独立抓到 `:471` 与表格的矛盾，成立）→ Sonnet 机械验证（8 个 PR 的合并状态
逐个 `gh pr view` 实查、24 条标签与汇总表逐格对账、M1 八条状态、两条「等人拍板」的结案标记与
分支表同步）→ **Opus R2 未跑（纯台账）；Codex R3 未跑；R4 未跑**。*

### 自评

- **DeepSeek v4-flash 本轮：4/5 —— 今晚最高分。** 它独立抓到了 `:471` 的汇总行与表格矛盾（「tasks.md summary line still says 7 merged/1 in review despite the table showing all 8 DONE」），**完全成立，而且这就是这个 PR 里唯一实质的问题**。另一条（progress.md 提到 checks.ts 的哨兵改动而 diff 里没有）方向也对——那确实是引用了别的 PR 的成果。
- **这是它连续第二次在台账类 PR 上独立发现真实矛盾**（上次是 #153 的「11 vs 9」）。形态很清楚：**同一份文档内部的交叉比对它已经会做了**，缺的仍是和仓库真实状态比对。
- **下次怎么榨出更多信号**：把它已经会的那一半用到底——在 prompt 里要求「列出文档中每一个数字/计数/状态断言，并指出文中还有哪些地方陈述了同一件事，逐对比较」。它自己已经在做这件事，明确要求会让它做得更全。
