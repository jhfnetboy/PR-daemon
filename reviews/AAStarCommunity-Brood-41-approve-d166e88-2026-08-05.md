## Verdict: APPROVE（首轮，head `d166e88b`）—— **纯调研文档，方法和结论都站得住**

按纯文档 PR 的口径：只对**实质性错误**拦。核实过，没有。

### 我能核实的部分都核实了

- **结论表算术**：`$160 + $30–76 = $190–236`，文中写 `$190–240`（上界取整）。✅
- **「PR #36 前后评了 6 轮，每轮都带完整上下文重新进入」**——实查 GitHub：`clestons` 在 #36 上共 **7 条 review**（6 条 CHANGES_REQUESTED + 1 条 APPROVED）。文中说「6 轮」指的是 6 轮打回，和实际一致（第 7 条是最终放行）。**这条用来估 token 消耗的依据是真的。** ✅
- **交叉引用**：`README.md` ↔ `cost-analysis-api-vs-coding-plan.md` 双向链接，两个文件都在。✅

### 这份分析的方法我认可

- **把「日常开发（交互式）」和「PR 评审 / 无人值守（程序化）」拆开定价**，而不是找一个方案覆盖两者——这个切分是对的，因为两者的用量形态完全不同（一个吃交互配额，一个吃 token 且能吃到缓存折扣）。
- **拿本仓库的真实评审形态做依据**（「PR #36 前后评了 6 轮，每轮都带完整上下文重新进入」）而不是拍脑袋估 token——这是可核实的，我核了。
- **`| Kimi K3 | dashboard 才可见,文档不公布 | … | 未知,待查 |`** —— 查不到就写「未知，待查」，没有为了填满表格而编一个数。这一点很重要。
- **末尾留了「待验证」表**（`OpenRouter GLM-5.2 的实际质量 | 价格便宜 3 倍,但可能是量化档 | 拿本仓库真实 PR 做对照评审`），把「便宜 3 倍」这条最诱人的结论标成待验证而不是直接采信。

### 非阻塞

- **[Low] 「同样的钱，配额和能力都比现在这一份 $200 强」这句在区间上界不成立。** `$190–240` 的低端确实更省，高端 `$236` 比 `$200` 贵约 18%。建议改成「**低端更省、高端略高，但配额和能力都更强**」——现在这个写法读起来像「白赚」，而实际上高端是「多花 18% 换更强配额」，那也是个好交易，只是别把它说成不花钱。
- **[Low] 外部牌价会过期。** 文中已经标了「分析日期：2026-08-05」，这点做得对；建议再补一句「牌价以各家官网为准，本文数字截至该日期」，免得三个月后有人照着这张表做决定。
- **[Low] 缓存命中率 92% 是引用 OpenRouter 的实测，不是本仓库的。** 而成本估算里 `Kimi K2.7-code(缓存命中) $0.051 → ~$31/月` 这个最优结果依赖它。本仓库的评审形态是「每轮带完整上下文重新进入」——这**有利于**缓存命中（前缀高度重复），但命中率会不会到 92% 是另一回事。建议把「~$31」标成**乐观档**，和已有的「无缓存 ~$76」并列成区间来读。

### Assumptions

- 在独立 worktree（head `d166e88b`）里核对，未改动 `~/Dev/aastar/Brood` 任何文件。
- **各家厂商牌价我没有独立核实**——那需要访问外部定价页，超出评审能核的范围，而且文档自己标了分析日期。我核的是：**内部算术、对本仓库状态的断言、交叉引用**这三类可查的东西。按纯文档口径，「需要更多证据」不构成阻断理由。
- **R2/R3/R4 未跑** —— 纯调研文档，零代码，无争议项。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons（`$pr` v4，**2 轮 + 机械验证**，首轮 head `d166e88b`）：DeepSeek R1a
（`deepseek-v4-flash`；triage 判定 docs-only 准确，无 finding）→ Sonnet 机械验证（结论表算术复算、
`gh api` 实查 PR #36 的 review 条数与状态分布验证 token 估算依据、双向交叉引用可解析性）→
**Opus R2 未跑（纯调研文档）；Codex R3 未跑；R4 未跑**。*

### 自评

- **DeepSeek v4-flash 本轮：3/5。** 无 finding，triage 准确（「Documentation-only PR adding a cost analysis… No code changes, no security or logic impact」）。对这类纯外部调研文档，不硬凑 finding 是对的。
- **下次怎么榨出更多信号**：调研类文档最值得查的是**它对自己仓库的断言**（这里是「PR #36 评了 6 轮」）——那是唯一能被本地数据证伪的部分。下次在 prompt 里加一条：「找出文档里所有关于**本仓库**的可核实断言（PR 编号、轮次、文件、分支状态），逐条列出并说明怎么验」。外部牌价它验不了，但这一类它能指出来该验什么。
