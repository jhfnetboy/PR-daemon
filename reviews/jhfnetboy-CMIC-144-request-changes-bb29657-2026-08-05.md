## Verdict: REQUEST_CHANGES (incremental re-review, head `bb29657`, round 2)

**上一轮 4 条 High 全部真修了**，我拿评审里点名的 18 个字符串逐条打过新实现：裸否定（`不出图了`/`我不想出图`/`没必要出图`/`不打算出图`）、疑问（`出图收费吗`/`什么时候出图`/`出图要多久`/`怎么出图`/`就这个吗`）、触发词无边界（`做个图大概多少钱`/`就这个价格太贵了`/`就这个尺寸不对`…）、匹配点之后的反悔（`go ahead, actually no, hold off`/`就这个，算了不用出图了`…）—— **18/18 全对，另外 5 个正向用例仍正常触发，0 不符**。作者自己的测试也全绿。

「扁平正则表达不了否定作用域，换分句器」这个判断是对的，方向也对。

**但这次重写引入了两条回归，我用新旧两个 head 对跑证实的 —— 其中一条正是这个文件自己声明「绝不能错的那个方向」。**

### 🔴 Blocking

1. **[High] 回归：中文最常见的同意说法全部失效。** `render-gate.ts:109` 的 `clauses()` 有意把 `。！？` 留在分句里，而 `:131-134` 新加的中文触发词又以 `$` 结尾 —— 两个改动各自看都对，合起来互相抵消。
   **两边对跑，8/8 全翻转（`4bd3229` → `bb29657`）**：
   ```
   出图吧。      true → false        直接出图。      true → false
   就这个吧！    true → false        好的，出图吧。   true → false
   出图吧!      true → false        出图。         true → false
   就这个吧👍    true → false        出图吧🙏       true → false
   ```
   带一个正常句号/感叹号/emoji 就不认了 —— 而这恰恰是中文聊天里下单最自然的写法。测试套件里每一个中文正向用例都是**不带标点**的，所以全绿。
   **修法**：别用裸 `$`，改成容忍尾部标点的前瞻 `(?=[\s。！!?？…~"'』」》)】\u{1F300}-\u{1FAFF}]*$)/u`；或者把「分句正文」和「分句终止符」拆成两个字段，让锚点判断和疑问判断各取所需 —— 后者能消掉这一整类，而不是补两个实例。

2. **[High] 回归，且是烧 GPU 的方向：`will` 从疑问词表里消失了。** 对比两个版本的正则，一眼可见：
   ```
   旧 :98   …can|could|would|will|is|are|am|may…
   新 :158  …can|could|would|is|are|am|may…      ← will 没了
   ```
   实测：`Will you go ahead and render it` / `Will you make it red` / `will you sample this`
   —— **旧版 false（正确识别为疑问），新版 true（真去出图了）**。
   一句礼貌的英文问句现在会直接烧一次 GPU。这个文件 `:95-98` 自己写着「误判 = 烧 GPU + 擅自行动，拿不准一律不算指令」，这条回归正好踩在它声明要保护的方向上。前面三轮都没抓到。
   **修法**：把 `will` 加回去，顺带考虑 `won't|shall|wanna`。

3. **[High] 上一轮 High-4（匹配点之后的反悔）只修了一半 —— 只在逗号分隔时生效。**
   `clauses()` 只按 `。！？!?；;,，、\n` 切，**不切** ASCII `.`、`—`、`…`、`:`、破折号，也不切纯空格；而 REVERSAL 只扫**指令分句之后**的分句。新旧两版都是 true：
   ```
   go ahead. no wait.        Render it. Actually no.     go ahead — no wait
   go ahead… actually no     go ahead: no, skip it       go ahead no wait
   ```
   上一轮那 4 个例子恰好都用了 `,` 或 `？`，所以语料本身对这个缺口是结构性失明的。这也是烧钱方向。
   **修法**：分隔符补 `.`（要求后接空格/行尾，别切坏 `3.5cm`）、`—…:/()`；并且在定位到指令分句后，**再扫这个分句自己触发词之后的尾巴**——否则 `go ahead no wait` 这种完全没有标点的仍然漏。

4. **[High] 上一轮 High-3 的边界修复只应用到了中文，英文触发动词仍是无边界子串。** 新旧两版都 true：
   ```
   make it cheaper                        I want to show it to my boss first
   take this into account                 go with this approach later maybe
   ```
   在一个报价对话里这些都是谈规格/谈价钱，不是出图指令。
   **修法**：给英文动词做和中文同样的收尾锚定，或把 `take this` 从裸动词表里去掉。

5. **[Med] 测试套件不具备判别力 —— 变异测试 8 个变异体活了 5 个。** 其中包括**把中文触发词的锚点整个去掉（等于把上一轮 High-3 的修复原样退回）测试依然全绿**，还有：把多分句 REVERSAL 循环截断到深度 1、删掉 REVERSAL 里的裸 `\bno\b`、把 `INTERROGATIVE_ANY` 缩到只剩 `什么|怎么`（`多久`/`多少`/`哪个`/`哪种`/`几天` 从没被任何用例走到）。
   语料是从词表里抄出来的，它证明的只是「正则匹配它自己那张表」。
   **修法**：把上面 1/2/3 里的字符串全部加成 fixture，并要求每个列出的变异体都必须让测试变红。

### 非阻塞

- **[Med] `render-gate.ts:147`** — REVERSAL 里的裸 `\bno\b` 会把肯定语气当成反悔：`go ahead, no problem` / `render it, no problem` → false。按本文件声明的不对称（漏判只是让客户再点一次），这是便宜的方向，所以是 Med 不是 High。建议要求 `no` 处于分句开头，或后接停止词。
- **[Med] `render-gate.ts:191`** — 「指令 + 补充说明」被读成反悔：`出图吧，不用太精细` / `出图吧，不用改了` / `就这个吧，别的不用管` / `出图吧，别忘了加logo` → 全 false。而「指令 + 限定」恰恰是下单最自然的说法。
- **[Med] `render-gate.ts:160-165`** — `isQuestion` 是分句局部的，加一个逗号就翻转结论：`做个图大概多少钱` → false（上轮修好的），但 `做个效果图吧，多少钱` → **true**。同一个意思，相反的花钱决定。
- **[Low] `render-gate.ts:131`** — 末尾锚定造出了它所修 bug 的镜像：`价格就这个` / `预算就这个` → true。
- **[Low] `render-gate.ts:176`** — `-2000` 的尾部截断会砍掉分句开头的否定词：`'别' + 'x'×2100 + '出图吧'` → 旧版 false，新版 true。需要单个分句超 2000 字符，很牵强，但方向是烧钱那边。
- **[Low] `render-gate.ts:133`** — `做张图` 两版都 false，`(?:一张|个)?` 漏了裸 `张`。

### 驳回的 findings

- **R1a「`不知道` 和 NEGATORS 冲突」** —— 实测 `不知道做个图要多少钱` → false，正是期望结果，不成立。
- **R1a「`go ahead, no thanks` 被拦」** —— `no thanks` 本来就是反悔，按本文件声明的不对称，拦掉是正确行为。

### 一条方法建议

这次两条回归的共同根因是同一个设计选择：`clauses()` 把终止符留在分句正文里，于是下游每一处都得自己判断「这个终止符是信号（`？`）还是噪声（`。！`）」。**把分句正文和终止符拆成两个字段**，锚点问题和疑问检测问题就互相独立了，能消掉这一类而不是打两个补丁。

另外建议给这个文件加一个**双 head 差分测试**：任何重写都必须显式声明每一处行为变化，而不是等到 review 才发现。今晚这两条回归都是靠「拿旧 head 跑同一组字符串」抓出来的 —— 单看新版怎么都看不出它比以前差。

### Assumptions

- 在两个独立 worktree（`4bd3229` 与 `bb29657`）里对跑同一组探针，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件。
- 增量范围 `4bd3229..bb29657`（1 个 commit）。PR body 未关联 issue，跳过 Issue-compliance。
- **R3(Codex PK) 本轮未跑** —— 本会话内它已两次零输出挂起被杀（`codex_pk.sh` 的 360s 上限未生效）。
  所以本 review 标 **3 轮不是 4 轮**。上面每条阻断项都有我自己新旧两版对跑的机械证据，不依赖 Codex 背书。

---
*Reviewed by clestons (`$pr` v4, **3-round — Codex R3 未跑**, incremental `4bd3229`→`bb29657`）:
DeepSeek R1a+R1b（`deepseek-v4-flash`;3 条 findings,1 条成立但需降级,2 条实测不成立）→ Sonnet 机械验证
（新旧两个 worktree 对跑：上轮 18 个字符串 18/18 已修、中文标点 8/8 回归、`will` 词表缺失、
同句反悔新旧皆 true）→ Opus R2（独立评审,挖出中文锚点回归、同句反悔缺口、英文动词无边界,
并用变异测试证明套件 8 个变异体活 5 个）→ **Codex R3 挂起未跑** →
Opus R4（终裁 + 全量补扫,挖出前三轮都漏掉的 `will` 回归——唯一一条烧 GPU 方向的新回归）。*
