## Verdict: REQUEST_CHANGES（增量复审，head `b4c8954`，round 5）—— **两条，约 6 行编辑 + 一个空账本文件**

**上一轮两条阻断项都真修了，而且这次是彻底的。**

| round-4 的问题 | 现在 |
|---|---|
| `:607` §8 决策表第 7 行仍下达被 `spec.md:27` 禁止的指令 | ✅ 照第 6 行重写，并明写「口径以 `spec.md` §0 D-7 行为准」 |
| `:326` `leads` 正文同一条过期指令 | ✅ 改成「PII 扫描全仓自动、无需登记 —— 口径见 `../agent/spec.md`」 |
| 「建议起手顺序」5 行里 3 行过期 | ✅ 整张表删掉，换成 3 条**不会过期的规则** + 显式的「状态一律以标签为准」 |

我把 `docs/design/*.md` + `docs/agent/*.md` 里**每一处** PII-扫描表述重新拉了一遍：5 个设计稿位置 + `spec.md` + `roadmap.md` + `tasks.md` 5 处，现在**全部互相一致，零残留**。上一轮我用 `head -6` 漏掉的那两处，这轮是干净的。

新加的 `spec.md > §8` 优先级说明是**真正的结构改进**——它把上一轮的教训写成了规则，而不是又修一个实例。

台账本身也校对干净：**24 个 `### Tx.y.z` 标题**，逐条标签 `DONE 2 / PR_OPEN 2 / READY 4 / BACKLOG 16 / BLOCKED 0`，与汇总表**逐格相符**；每个 PR 编号/状态/日期都对（#144 确实 MERGED 进 `preview`，`c80cbec`，`2026-08-05T02:11:59Z`；#145/#146/#147 确实都还 OPEN）。

`package.json` 那条冲突提醒**精确且有用**，我实跑了三条串：`preview`(含 #144) 是 `…test:pdf && pnpm test:gate && pnpm test:insert…`，而 **#146 把 `test:slots` 插在 #144 放 `test:gate` 的同一个位置**，#145 在行首加 `test:ci-covers` 并插 `eval:chat`——同一行，两个都会冲突。「三条都留，别覆盖」是对的。

---

**卡住的两条是同一件事：这个 PR 做了一次状态翻转，只把它传播到了 3 处，还剩 8 处停在翻转前。**

### 🔴 Blocking

1. **[High] 本轮把 `T1.2.2` 翻成 `DONE`，但 `T1.1.2` 的【唯一】依赖就是 `T1.2.2` —— 它已经解锁了，标签却还是 `BACKLOG`。**

   ```
   tasks.md:45   T1.1.2 - **依赖**：T1.2.2   ← 唯一一条
   tasks.md:36   ### T1.1.2 消费 done + 出图触发闸  `BACKLOG`   ← 没跟着改
   ```
   我把另外三条 BACKLOG 也查了，**它们仍然是对的**：`T1.2.1←T1.1.2`、`T1.3.2←T1.3.1(#146 OPEN)`、`T1.5.1←T1.4.1(#145 OPEN)`。四条里**恰好错了一条**。

   **而这一条的危害是这个 diff 自己搭出来的**：新的起手顺序规则 2 说
   > 「其次做每天在漏钱的…**当前是 `T1.1.2`** 出图触发闸（客户打句 "Hi" 也会烧四次 GPU）」

   紧接着又说「**状态一律以上面每个 Task 条目自己的标签为准**」。于是 pilot 被指到 T1.1.2，读到 `BACKLOG`，把这份文档刚刚认定的「每天在漏钱」那一条**推迟掉**。这和 round 4 的 B1 是**同一个形状**：一条规则指向一处过期的字段，过期的那处反过来压过了正确的正文。第三轮了。

   逐处清点，这次翻转影响 **11 处，只改了 3 处**：

   | | 位置 | 现在写着 |
   |---|---|---|
   | ✅ | `tasks.md:69` | T1.2.2 → `DONE` |
   | ✅ | `tasks.md:449-451` | 计数 DONE 2 / PR_OPEN 2 |
   | ✅ | `progress.md:31,39-41` | 分支表 + PR 列表 |
   | ❌ | `tasks.md:36` | T1.1.2 仍 `BACKLOG` |
   | ❌ | `tasks.md:87` | T1.2.2 证据仍「PR #144（**等 daemon 评审**）」——标题已是 `DONE`（对比 `:33` 的 T1.1.1 就写对了：「已 APPROVED 并 squash 合入 `preview`」）|
   | ❌ | `tasks.md:454` | `READY 4`「均不在 M1 范围内」 |
   | ❌ | `tasks.md:455` | `BACKLOG 16`「均有明确前置」 |
   | ❌ | `progress.md:15` | 已交付仍只有 T1.1.1 |
   | ❌ | `progress.md:16` | 「M1 里所有无阻塞的 READY 都已进 PR，剩下的**全依赖未合并的 PR**」 |
   | ❌ | `progress.md:27` | 「`preview` 领先 main **1 个 commit**（`65c6927`）」—— 实测 `git rev-list --count origin/main..origin/preview` = **2**（`65c6927` #143 · `c80cbec` #144），而它就在本轮改过的那一行**上面一行** |
   | ❌ | `progress.md:64` | 「剩余四条（T1.1.2 / T1.2.1 / T1.3.2 / T1.5.1）**全部**依赖未合并的 PR」 |

   **修法**：`T1.1.2` → `READY`；计数 `READY 5 / BACKLOG 15`；`tasks.md:87` 证据行照 `:33` 写；`progress.md:15` 补 T1.2.2；`:16`/`:64` 改成「T1.1.2 已解除依赖、可开工，其余三条仍依赖 #145/#146」；`:27` 改成 2 个 commit。

2. **[High] `tasks.md:461` —「权威是 `.pilot/followups.md`（`pilot followups list`）」—— 路径和命令都不对，文件也不存在，而且【静默】失败。**

   对着真代码核过：
   ```
   本分支 .pilot.yml:11            docs_dir: docs/agent
   pilot 的 scripts/followups.sh:41  ledger="$docs_dir/followups.md"
   ```
   → 工具真正的账本路径是 **`docs/agent/followups.md`**，`.pilot/` 是这个工具**永远不会产生**的路径。而两个文件都不存在（`docs/agent/` 现在只有 `progress.md roadmap.md spec.md tasks.md`）。

   关键在于它**不报错**：
   ```sh
   followups.sh:113   list)  [ -f "$ledger" ] || { echo "(no ledger at $ledger)"; exit 0; }
   ```
   没有账本 → 打一行提示 → **exit 0**。`ensure_ledger` 只有 `add` 会调，所以不会自愈。

   而这段文字**主动把表内那份降格成副本、把账本抬成「权威」**。一个尊重这个次序的自动执行者会去跑 `followups list`，拿到 exit 0 和零行，正确的读法就是「没有待办」—— **FU-1..FU-5 四条在途工作静默消失**，没有报错、没有退出码、没有任何指回这张表的信号。

   **修法**：二选一 —— 要么把 `docs/agent/followups.md` 建出来并提交（内容就是 FU-1..FU-5），并把路径改对；要么去掉「权威」这个说法，让这张表就是记录本身。

### 非阻塞

- **[Low] `progress.md:39`** —「#142 docs ——**第二轮**意见已修完」，实际这是第 5 轮（GitHub 上 clestons 已有 5 条 review）。同一次编辑顺手改掉即可。（Codex 挑出来的。）
- **[Med] `agent-interaction-v2.md:669`** — 这条「`spec.md` 高于 §8」的规则**停在附录里、位置在 §8 【下面】**，挂在附录自己那条注的后面。一个直接落到 §8 的读者永远看不到它 —— 正是这条注自己诊断的那个失败（「权威引用只有在你能读到它时才有用」）。建议往 §8 标题下镜像一行。
- **[Med] 同上** — 它声明的适用范围（数据结构 + D-7/D-3）**比它给出的理由窄**（理由是「§8 是人手维护的汇总，`spec.md` 才是实现直接对照的那份」）。范围之外（例如 59 美元口径）没有仲裁者。要么补一句「本条外仍以 §8 为准」，要么去掉范围限定。
- **[Low] `tasks.md:459-470`** — 这个 PR 的论点是「重述的那份总是先过期」，并据此删掉了两张重复表；同一个 commit 又加了一张手工维护的 FU 状态副本。等 `docs/agent/followups.md` 真的存在之后，建议把状态列砍掉，只留指针。
- **[Low] `tasks.md:466`** — FU-2「归入 T1.1.2 一起做」继承了 T1.1.2 的错误标签，于是 FU-2 也读成被挡住。属于阻断项 1 的下游。

### 驳回

- **R1a「FU-2..FU-5 标 OPEN 但没有 PR 链接」** —— 标 OPEN 按定义就是还没有 PR，没链接不是缺陷。
- **R1a「`followups.sh` 不在 diff 里，需确认脚本存在」** —— 方向对但没得出结论：脚本存在且正确，缺陷在文档写的路径和命令上（已并入阻断项 2）。
- **「新的起手顺序又把 Task 编号写死了（当前是 T3.1.1 / T1.1.2）」** —— 我认真考虑过，**不成立**。旧表的问题是**可操作内容就是那份枚举**；这里可操作内容是三条规则，Task 名带着「当前是」且有显式的「以标签为准」兜底。T3.1.1 现在是 `READY`，规则 1 是对的。真正的缺陷是它委托过去的那个字段（标签）错了 —— 也就是阻断项 1。

### 一条给作者的方法建议

三轮下来，反复出问题的**不是某一处措辞，而是同一个动作：这份文档加权威指针的速度，快过它维护被指向那处的速度**。round 4 是 §8，这一轮是「标签字段」和 `.pilot/followups.md`。文档已经在正文里**正确诊断过这个毛病两次**，同时在结构上又犯了两次。

两条机械化的收口，都不需要判断力：

1. **合并是一次状态迁移，不是一次改行。** 把 T1.2.2 标 `DONE` 有 11 处下游后果，本轮传播了 3 处。合并时的动作应该是「翻标签 → **重解每一个 `依赖` 里点了它的 Task** → 复核两句汇总注解 → 更新 `preview` 领先数」，而不是「翻标签」。
2. **一条 pre-push 检查**：`docs/agent` + `docs/design` 里每一处「权威是 X」/「以 X 为准」，X 必须能解析到一个存在的路径。这条本可以纯机械地抓住阻断项 2。

另外 `tasks.md:33`（T1.1.1）和 `:87`（T1.2.2）正好是同一件事的正反例：把**标签翻转和证据行绑成一次编辑**，本轮整个阻断集合都不会发生。

### Assumptions

- 在独立 worktree（head `b4c8954`）里 grep/读全部 `docs/agent/*.md` 与 `docs/design/*.md`，对照真实的 `.pilot.yml`、pilot 的 `scripts/followups.sh`、`git rev-list origin/main..origin/preview` 与 GitHub 上 #144/#145/#146/#147 的真实状态，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件。
- 按纯文档 PR 的口径，只对**实质性错误**判阻断。上面两条分别是「路径指向不存在的文件」和「被仓库自身状态推翻的断言」，两条都**静默地误导自动执行者**（一条把它从这份文档自己认定的漏钱项引开，一条让 follow-up 停止条件读成 0），所以判实质性；措辞、位置、重复表全部降级为建议。
- **本轮已是第 5 轮，我认真权衡过「按建议放行」**：结论是不放行，因为这两条**都是本轮自己新引入的**（Follow-up 那一节是新加的；那些断言在本轮翻转 T1.2.2 之前都还是真的），不是评审在移动标准 —— round 4 的两条真的修好了。修完约 6 行编辑加一个账本文件，没有新内容要写。
- **R3 Codex 跑通了**（`gpt-5.5`，本会话首次成功），三条 CONFIRM 并补出「第二轮」那条。⚠️ **但它当时无网络**（28 次 `api.github.com` DNS 失败），所以它是**对我提供的证据**做的确认，不是独立读仓库；它对 F3 的确认自己也标了条件（"If #144 is merged…"）。上面每一条的实证输出都是我自己跑的。

---
*Reviewed by clestons（`$pr` v4，**4 轮**，increment `4137b7e`→`b4c8954`）：DeepSeek R1a+R1b
（`deepseek-v4-flash`；2 条 Low，1 条方向对但未得出结论、已并入阻断项 2）→ Sonnet 机械验证
（逐条 grep 复核上轮 2 条阻断项与全仓 PII 口径一致性、24 条标签与汇总表逐格对账、三条 `check` 串
实测确认冲突提醒精确、#144-#147 真实状态、`rev-list` 实测 preview 领先 2）→ Opus R2（独立评审，
挖出 T1.2.2→DONE 这次状态翻转只传播了 3/11 处，且 T1.1.2 的唯一依赖恰好就是 T1.2.2，
以及 `.pilot/followups.md` 路径错+文件不存在+`list` exit 0 静默）→ Codex R3（三条全 CONFIRM，
补出 `progress.md:39`「第二轮」过期；**无网络，基于所给证据确认**）→ Opus R4（终裁 REQUEST_CHANGES，
全文件扫描又补出 `tasks.md:87` 证据行与 `progress.md:15` 已交付行两处未传播，并逐一核实了台账指向的
全部代码锚点均正确）。*

### 自评

- **DeepSeek v4-flash 本轮：2/5。** 两条都是 Low：一条（FU 无 PR 链接）实测不成立，另一条（`followups.sh` 是否存在）**方向恰好对准了本轮第二条阻断项，但它只说了「verify script exists」就停住了** —— 没去读 `.pilot.yml` 的 `docs_dir`，也没发现路径根本对不上。这正是 flash 的典型形状：能嗅到可疑的引用，不会顺着配置往下解析一步。本轮 11 处未传播的状态它一处没提。
- **下次怎么榨出更多信号**：对文档类 PR，把仓库的配置文件（`.pilot.yml` / `package.json` scripts）连同 diff 一起喂给 R1，并在 prompt 里明确要求「每一处文件路径/命令都判定是否真实存在」—— flash 做这种逐项核对是可靠的，它弱的是自己想到要去核对。
