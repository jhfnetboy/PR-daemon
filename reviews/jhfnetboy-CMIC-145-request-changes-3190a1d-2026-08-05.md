## Verdict: REQUEST_CHANGES (incremental re-review, head `3190a1d`, round 3)

**上一轮三条 High 全部真修了**，逐条实跑：

| 上轮问题 | 现在 |
|---|---|
| `\b` 挨着中文恒假，CUR 里中文货币词全死 | ✅ `20 元`/`12 美元`/`人民币 12`/`这个盒子 20 元。`/`我们这边 12 元/个。`/`500 units MOQ.`/`500 个起订` **7/7 拦住**；`[^\D]` 双重否定笔误也修了 |
| A1 两个方向都错 | ✅ 问句里的短语不再算建议（3/3），真建议 + 礼貌请求不再算无建议（2/2） |
| A6 招呼词表漏 太好了/Love it/Perfect | ✅ 4/4 抓住 |
| 无回归 | ✅ A4 推辞话术 3/3 放行、真报价 6/6 拦截 |

`ci-covers` 那条注释假阴性也修了 —— 我重跑了原来的复现，现在正确 exit 1。

**但同一个模式第三次出现：每个修复都是往白名单加一个字面量、或在正则里挖一个 lookaround，而失败随即挪到相邻的说法上。这轮有一处甚至把上一轮修好的东西带了回来。**

### 🔴 Blocking

1. **[High] `checks.ts:52,94` — 逗号切分让「这一句是提问」的守卫只看得到最后一段，于是任何带逗号的选项式提问都被算成「有建议」。** 实测：
   ```
   判成有建议 ❌  「你觉得哪个盒型最适合，磁扣还是天地盖？」
   判成有建议 ❌  "Which one is the best fit, matte or gloss?"
   A1 触发  ✅  「你觉得哪个盒型最适合?」        ← 不带逗号的才对
   ```
   **这是上一轮 High（A1 误绿）被修它的那次改动重新引入的**：golden 只钉住了不带逗号的那个变体。而选项式提问（「A 还是 B？」）恰恰是最自然的问法，所以一整段纯提问的对话可以在 A1 和 A5 上全绿 —— 正是那条 golden 用例本来要防的结果。

2. **[High] `checks.ts:202` — 「块」作量词被当成价格。** `毛`/`钱` 是可选的，于是：
   ```
   误判价格 ❌  「盒盖用 2 块卡纸对裱」  「分成 3 块区域」  「这个结构有 4 块板材」
   ```
   「块」用于板材/区域是包装结构讨论里的常用词，而 A4 决定 live 模式的退出码 —— 这是自己给自己制造的 live 失败。
   **修法**：要求有金钱线索，例如 `\d+\s?块(?:\s?\d\s?(?:毛|角))?(?=\s?(?:钱|一个|每个|一只|左右|上下)|$)`。

3. **[High] `checks.ts:194-224` — A4 只认阿拉伯数字、单位表也偏窄，真报价整类逃逸。** 实测 4/4 全放行：
   ```
   漏掉 ❌  「大概三块五一个」  「开模费八百块」  「一般五百个起。」  "It runs about 3.5 a box."
   ```
   缺的是：中文数字（一二三四五六七八九十百千）、`月/月底/月中`、不带「起订/MOQ」而用「起」或 `minimum` 表达的 MOQ、以及 `\d\s+a\s+(box|pc)`。
   另外这轮新加的 `(?!\s+of\b)`（为了救 `2 weeks of samples`）把 `Delivery within 3 weeks of the deposit.` 也一并放走了 —— 修一句、开一句。

4. **[Med] `scripts/ci-covers-check.test.ts:42` — 注释剥离的修法没有关掉它声称关掉的那一类，而且新引入了一个误红。** 两个方向我都实测了：
   - **假阴性**：在 ci.yml 的**步骤名**里写 `- name: 以后再补 pnpm test:ghost`、实际 `run: echo skip` → **守卫 exit 0 通过**。原因是第二条过滤 `/^\s{6,}\S/` 会放行任何缩进 ≥6 空格的行，而这个 ci.yml 里 `- name:` / `if:` / `with:` / `env:` 全都是。
   - **假红**：合法的 `run: git log -1 --pretty="PR # 145" && pnpm eval:chat` → **守卫 exit 1**，报 `eval:chat` 缺失。（R1a 这条方向说反了：它只会删文本，所以只会造成误红、不会漏。）
   **修法**：`js-yaml` 工作区里已经有了 —— `load` 之后走 `jobs.*.steps[].run`，一次性消掉注释误红、`- name:` 假阴性和缩进启发式，代码还比现在这套正则管线短。（本仓已经有「CI yaml 要真解析」这条教训。）

### 非阻塞

- **[Med] A2 仍然两个方向都错。** 它只在「一句里同时有 `REQUEST_VERB` 里的祈使动词 **且** ≥2 个 `ENUM_MARK`（`\band\b` 也算）」时才触发。后果：
  - **误红 4/4**（正是原则第十一条要求的「先推荐、再问一件事」）：`Tell me the quantity and I'll refine the structure, and we can go from there.` / `Send me the logo and I'll mock it up, and we can iterate.` / 「我建议用磁扣盒。告诉我数量,我来算,然后我们再定。」 —— `\band\b` 分不出「又一个索取」和「我自己的下一步」
  - **漏掉 6/6** 不用那几个动词的连珠炮：`What size, how many, what budget, and when do you need it?` / `I need the size, the quantity, the budget and the deadline.` / `Please share size, quantity, budget, timeline.` / 「麻烦把尺寸、数量和预算发一下。」
  **建议的形状**：数**被索取的信息槽**而不是数分隔符 —— 切分句子，丢掉主语是助手自己下一步的分句（`I'll` / `we'll` / `我来` / `我这边` / `回头`），在其余分句里数不同的槽（尺寸/数量/预算/交期/城市/logo/材质/用途），≥2 判红。这样也能顺带并掉 `questionCount`（连珠炮问句就是 ≥2 个槽）。
- **[Med] `checks.ts:153-164` — A6 的招呼豁免是封闭词表，且只看前 2 个分句，两个错误都指向假绿**：`Happy to help! What size do you need?` / 「谢谢你的信息！你需要多大尺寸？」/ 「没问题！你想要什么盒型？」/ `Hi! Great! Sure! What size…`（3 个招呼超过 `i < 2` 上限）**都没被抓**。建议反过来：跳过所有「短、无 `?`、不含槽名词」的前导分句，而不是匹配招呼白名单。
- **[Med] `checks.ts:221` — `天(?!时间)` 只排除了字面的「3 天时间」**，「给你 3 天想想颜色」仍判 `lead-time`。文件注释说这个案例修好了，实际只修了那一种措辞。
- **[Med] 变异测试：72 个谓词变异存活 29 个（击杀率 60%，比上轮的 ~32% 好，但存活集是系统性的）。** 最差的是 A4 簇：**21 个检测器里有 13 个可以单独删掉而 golden 全绿** —— `[$¥€£]\s?\d`、块 分支、三条 MOQ、`min order`、英文每单位、`单价/售价/报价/开模费`、`每个 N`、`N/pc`、整条中文交期分支、`交期 N`、`lead time N`。根因是结构性的：`bad-price-leak` 把 price+MOQ+lead-time 打包在一条回复里，而 A4 每轮只 push 一条违规，于是每个模式都在替其它模式打掩护。**修法：一条 fixture 一个信号，21 条最小用例各只触发一个 A4 模式。**
- **[Med] `checks.ts:262-264`** — `if (q > 1) … else if (hasStackedRequest(…))`：一旦 `q > 1` 命中，stacked-request 谓词永远不会被执行。这和上一轮已经在 A3 上修掉的「两个谓词互相掩护」是同一个形状。
- **[Low] 其它未钉住的谓词**：A2 声明的问句上限没被钉（`q > 1` 改成 `q > 2` 存活，没有 fixture 恰好 2 个问句）；A5 的「连续」语义没被钉（`i>0 && !advice && !adviceFlags[i-1]` 改成 `i>0 && !advice` 存活，A5 可以静默退化成 A1 的副本）；A6 的 `i<2` 窗口、30 字符招呼上限、`isGreeting` 里的 `?` 守卫全部未钉。
- **[Low] `ci-covers-check.test.ts:69` — `viaCheck` 是个全局逃生舱**：ci.yml 里任何一处出现 `run: pnpm check`，所有步骤就都算通过。今天是潜在的（ci.yml 是逐条列举的），但这意味着「把 ci.yml 简化一下」这个最自然的动作会静默停用整个守卫。
- **[Low] `checks.ts:237-244`** — A3 只数 CJK 字数和空格分词数，用标点连起来的 chip 两边都逃：`matte-black-with-gold-foil-and-spot-uv-finish`（44 字符、1 个「词」）不算超长。chips 是按钮，建议按显示宽度（cjk×2 + ascii）判。

### 驳回的 findings

- **R1a「`ADVICE_IS_REALLY_A_REQUEST` 仍会匹配 建议你把 logo 文件发给我」** —— 这个否决现在是**按分句**的、且是刻意的；整轮仍然通过前一个分句判为有建议（实测 2/2）。
- **R1a「ci-covers 的 `#` 剥离可能漏掉」** —— 方向反了，它只会删文本，所以只会**误红**不会漏（我复现了误红那一面，见上面阻断项 4）。

### 一条方法建议

三轮下来这个文件的模式已经很清楚：**A1/A2/A6 是语义谓词的词法代理，会一直来回震荡**。文件自己已经写了「A1 的分数只该当趋势看，别当验收线」—— 这句话应该由**运行器**强制，而不是只写在注释里：A1/A2/A6 不应该 gate `pnpm check`，而 A4（唯一必须可靠的那条）应该围绕**槽位抽取 + 数字归一化**重建，而不是表层正则。

另外，golden 现在是按「过往评审发现」一条条追加的**变更日志**，而不是覆盖谓词的**矩阵** —— 这就是击杀率只有 60%、且存活集**全部落在还没有评审攻击过的模式**上的原因。fixture 集永远只能和上一次 review 一样好。把 A4 改成一信号一用例 + 把变异脚本接进 `check`，这把尺子才会自我防御；否则第四轮还会再找出 13 条。

### Assumptions

- 在独立 worktree（head `3190a1d`）里跑 eval、变异与探针，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件；临时改动已还原。
- **R3(Codex PK) 未跑** —— 本会话内它已两次零输出挂起被杀。**R4 未跑** —— 四条阻断项都有我自己实跑的
  正反两面输出。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons (`$pr` v4, **2-round + 机械验证**, incremental `a8c4b8c`→`3190a1d`): DeepSeek
R1a+R1b（`deepseek-v4-flash`;3 条中 1 条成立(adviceUnits 逗号切分,但严重度被低估)、1 条方向说反、
1 条驳回）→ Sonnet 机械验证（上轮三条 High 逐条实跑确认已修、A2 双向探针、CI 守卫的假阴性与假红各自
复现）→ Opus R2（独立评审,发现逗号切分把上轮修好的 A1 误绿带了回来、「块」作量词被当价格、中文数字
报价整类逃逸,并用 72 个变异证明 A4 的 21 个检测器有 13 个可单独删除而不被发现）→
**Codex R3 挂起未跑；R4 未跑**。*
