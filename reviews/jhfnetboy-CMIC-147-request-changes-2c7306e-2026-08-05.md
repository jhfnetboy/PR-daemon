## Verdict: REQUEST_CHANGES (incremental re-review, head `2c7306e`, round 3)

**上一轮两条阻断项都真修了，而且没有回归** —— 41 条探针零不符：

| 上轮问题 | 现在 |
|---|---|
| 裸「微」误伤 | ✅ `微调尺寸`/`稍微大一点`/`微压纹`/`轻微磨砂`/`微光泽膜`/`细微烫金`/`微雕工艺` **7/7 放行**，而 `微信`/`加微信`/`企业微信` **3/3 仍拦** |
| 英文价格/MOQ 回归 | ✅ `Unit price 2.8`/`Price 2.8`/`Quote 2.8`/`Min order 500`/`Minimum order 500`/`Lead time 15` **6/6 拦截**，和中文孪生（`单价 2.8`/`售价 2.8`/`起订 500`/`交期 15 天` 4/4）对齐 |
| 无回归 | ✅ 第一轮 9 条绕过仍全拦；12 条合法 chip 仍全放行 |

`test:api` 从 `&&` 链改成循环也是对的——8 个测试现在真的全跑，中间挂了后面还继续。变异测试确认新加的英文 price 模式**已被钉住**（删掉变红），存活的正则子句从 8/26 降到 4/28。

**但新模式同时太松和太紧，方向相反、证据对称。**

### 🔴 Blocking

1. **[Med] `chat.ts:148` — `[^\d]{0,4}` 窗口把「没有值的问句型 chip」吃掉了。** 实测 6/6 全被误伤：
   ```
   拦截 ❌  "Quote 2 options"   "Price 2 colors"   "Quote 3 sizes"
   拦截 ❌  "Price A5 size"     "Quote A4 box"     "Price v2 layout"
   ```
   窗口把**纸张规格**（A5/A4）、**版本号**（v2）、**选项个数**（2 colors / 3 sizes）都当成了金额。这和这个文件自己的 D-3 论证直接冲突（`:112` 附近写着：问句型 chip 要留着，它们是通往确定性报价引擎的漏斗），也会重新打开文件头警告过的 `chips: []` 级联 —— 而 `Price 2 colors`、`Quote 3 sizes` 恰恰是这个 agent 最常给的形状。

2. **[Med] `chat.ts:148` — 同一个窗口对**真的报价**又太紧。** 实测 4/4 全部逃逸：
   ```
   放行 ❌  "Price is about 2.8"   "Our price will be 2.8"
   放行 ❌  "Price: around 2.8"    "Cost 2.8"（裸 cost 不在候选里，而裸 price 在）
   ```
   **修法（一次解决 1 和 2）**：不要按「关键词邻近」判，按**值的形状**判 ——
   ```js
   /(?:price|quote|cost)\b[^\d]{0,12}(?:[$¥€£]\s?\d|\d+\.\d+|\d[\d,.]*\s?(?:usd|rmb|cny|eur|gbp|元|块|毛))/i
   ```
   上面 6 条误伤**一条都不含小数或货币**，4 条逃逸**每条都含**。这个改法两边一起关。

3. **[Med] `chat.ts:136` — 货币代码**前置**的报价整类逃逸。** 实测 4/4：
   ```
   放行 ❌  "USD 2.8"   "RMB 2.8"   "CNY 500"   "EUR 3"
   ```
   而测试里钉住的是镜像形式 `2.80 USD`（拦得住）。这和上一轮那条回归是**同一类不对称，只是换了一根轴** —— 上轮是中英不对称，这轮是「数字在前 / 代码在前」不对称。

4. **[Med] `chat.ts:169` — `tg` 不在品牌表也不在 `\b(?:vx|wx|qq|ig)\b` 缩写表里。** 实测 3/3 逃逸：`Tg @seller` / `tg: seller01` / `Add on TG`。`tg` 是中文 B2B 里 Telegram 最常见的写法，正是第一轮因为 `vx:`/`wx:`/`加V` 而判阻断的那个缩写类别。

### 非阻塞

- **[Low] `package.json:12` — `rc` 从未初始化，`exit ${rc:-0}` 会读到继承自环境的 `rc`。** 实测：`rc=1 sh -c '...'` 在**全部测试通过**的情况下仍 `exit 1` → CI 假红。修：脚本开头加 `rc=0;`。
- **[Low] `package.json:12` — 没有每文件失败标记。** 8 个测试现在都跑了（这是改进），但第 3 个挂掉后会被后面 7 个的 ~146 条断言刷屏，唯一信号只剩退出码；原来的 `&&` 链至少会停在出错的那个文件。建议 `|| { rc=1; echo "FAILED: $f"; }` 再加一行汇总。
- **[Low] `chat.ts:154` — MOQ 的放宽只做了一半。** `Min. order 500` 现在拦得住，但 `Min qty 500` / `Minimum quantity 500` / `Min-order 500` / `MinOrder 500` / `Starting at 500 pcs` 仍放行 —— `\s+order\b` 还是要求字面的 `order` 且前面必须是空白。
- **[Low] `chat.ts:142` — 新加的 `\b\d[\d,.]*\s?(?:cents?|usd|rmb)\s?(?:each|per…)\b` 基本是死代码。** 差分测下来，只有 `80 centseach` / `3 usdper pc` 这种没空格的畸形串才由它独占命中；`80 cents each`、`2 USD each`、`0.85 RMB per unit` 在这个 diff 之前就已被 `:136` 拦住了。
- **[Low] 剩余 4 条变异存活，其中 3 条是被遮蔽而非没测**：`每(?:个|只|件)\s?\d`（钉它的串 `每个 8 毛` 其实是被 `毛` 拦的，改用 `每件 8`）、`交期[^\d]{0,6}\d`（`交期 15 天` 是被 `天` 拦的，改用 `交期 15`）、`\b1[3-9]\d{9}\b`（被默认的 `redactContact` 遮蔽）。
  最后这条值得单独说：`chips.test.ts:113` 的 noop-redact 用例列了 4 个串，唯独漏了**真正需要它的那个** —— 加一个 `'13800138000'` 就能把这条钉住，而它存在的全部理由正是「`redactContact` 只能关掉 9 条绕过里的 1 条」。

### 我自己收回的一条

我先前报「破坏 `企业微信` 模式测试仍全绿 = 没测到」。**这条不成立**：`企业微信` 本身包含 `微信`，那个分支不可达，任何测试都钉不住它。正确的处理是**删掉这个冗余分支**，而不是给它补测试。同理 `unit\s*price` 对裸 `price` 也是冗余的（除非你也打算认 `unitcost`）。

### 一条方法建议

黑名单现在是 4 张表 28 条手工正则，而三轮的证据是它**漂移得比被 review 更快**：第一轮发现中文缺口，第二轮发现英文回归，第三轮发现货币代码前置、`tg`、`Min qty`、裸 `cost`。

`chips.test.ts:81` 那张中英**成对**表是对的直觉，但它只钉住了有人想到的那 12 对。更耐久的做法是：一个语料文件（`safe[]` / `unsafe[]`）+ 一条 CI 断言「变异存活数必须为 0」—— 每加一条正则就必须自己挣一个测试。

另外建议加一条显式测试：**一个真实的 4 条 chips 模型响应，过滤后不得为空**。`chips: []` 的风险现在是具体的而不是理论的 —— `Price 2 colors`、`Quote 3 sizes`、`Price A5 size` 正是最常被生成的形状，而文件头已经写明全过滤轮次在前端有已知副作用。

### Assumptions

- 在独立 worktree（head `2c7306e`）里跑测试、变异与探针，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件；变异已还原。
- **R3(Codex PK) 未跑** —— 本会话内它已两次零输出挂起被杀。**R4 未跑** —— R2 独立判定 2-round，
  四条阻断项都有我自己实跑的双向证据。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons (`$pr` v4, **2-round + 机械验证**, incremental `26d7bb4`→`2c7306e`): DeepSeek
R1a+R1b（`deepseek-v4-flash`;指出 price 模式过宽 —— 方向对,8 条探针里 7 条不成立但 `Quote 2 options`
确实误伤,是个真发现的起点）→ Sonnet 机械验证（41 条探针确认两条阻断项已修且零回归、变异确认英文 price
模式已被钉住、实跑复现 `rc` 未初始化导致的假红）→ Opus R2（独立评审,把误伤扩成 10 条一类、挖出货币代码
前置整类逃逸与 `tg` 缺失,并驳回了我关于 `企业微信` 的判断）→ **Codex R3 挂起未跑；R4 未跑**。*
