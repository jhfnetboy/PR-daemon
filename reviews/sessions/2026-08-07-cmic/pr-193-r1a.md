FILES:
- apps/api/migrations/0009-render-jobs-kind.sql — new migration adds kind column
- apps/api/schema.sql — adds kind column, updates comments
- apps/api/src/index.ts — adds sweepUndelivered, cron logic, owner/kind changes
- apps/api/src/persist-all.test.ts — new test for render persistence
- apps/api/wrangler.jsonc — adds 5-min cron trigger
- package.json — adds persist-all to test suite

FINDINGS:
1. [Medium] apps/api/src/index.ts:489 — sweepUndelivered queries `owner_user_id IS NOT NULL` but new code sets `finOwner` to null for anonymous | include anonymous or adjust query
2. [Low] apps/api/migrations/0009-render-jobs-kind.sql:1 — migration not idempotent, comment acknowledges | add IF NOT EXISTS or document rerun risk

TRIAGE: significant — core logic change affecting render persistence and cron behavior

SKELETON:
The sweep logic correctly handles the new owner/kind split, but the query excludes anonymous renders which may be intentional. The migration's non-idempotency is documented.