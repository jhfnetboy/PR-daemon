FILES:
- apps/api/migrations/0008-shares-page-html.sql — new migration adds page_html column
- apps/api/schema.sql — adds page_html column to shares table
- apps/api/src/index.ts — passes pageHtml to createShare with size guard
- apps/api/src/share.test.ts — tests share without image, snapshot rendering
- apps/api/src/share.ts — makes image optional, adds pageHtml field
- apps/web/share.html — adds snapshot rendering CSS
- apps/web/src/customer.ts — renames mailer box label
- apps/web/src/funnel.ts — renames mailer box label
- apps/web/src/sample.ts — pre-generates share link, builds page snapshot
- apps/web/src/share.ts — renders pageHtml snapshot if present
- apps/web/src/texture.ts — changes default logo text

FINDINGS:
1. [High] apps/api/migrations/0008-shares-page-html.sql:2 — migration not idempotent, comment says so | make idempotent with IF NOT EXISTS
2. [High] apps/api/src/share.ts:236 — pageHtml stored without assertNoCommercial check | add assertNoCommercial on pageHtml
3. [Medium] apps/web/src/sample.ts:447 — pageHtml length check uses hardcoded 512*1024 | use SHARE_PAGE_MAX constant

TRIAGE: significant — core share logic change, security-sensitive PII handling

SKELETON:
The diff makes images optional for shares, adding a page snapshot path. The main concern is the snapshot's PII handling — the server-side assertNoCommercial check is only applied to the design field, not the pageHtml field. The client-side grep for currency symbols is the only guard, which is insufficient as a security boundary. Also, the migration is explicitly non-idempotent, which could cause deployment issues.