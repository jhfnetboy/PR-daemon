FILES:
- apps/web/src/chat-history.ts — drawer close behavior made conditional on desktop/mobile

FINDINGS:
[Low] apps/web/src/chat-history.ts:208 — `isDesktop()` not defined in this diff | verify it exists in scope

TRIAGE: significant — UI behavior change affecting navigation and overlay logic

SKELETON:
The change correctly distinguishes desktop persistent navigation from mobile overlay. Verify `isDesktop()` is defined and handles resize events; otherwise mobile users may lose the close behavior.