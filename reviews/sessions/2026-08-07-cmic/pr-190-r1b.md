SECURITY_FINDINGS:
1. [Medium] apps/api/src/index.ts:1201-1204 — page_html stored/returned unvalidated HTML from client | sanitize/validate server-side before persistence
2. [Medium] apps/web/src/share.ts:72-78 — pageHtml rendered via innerHTML without sanitization | sanitize HTML before innerHTML assignment
3. [Low] apps/api/src/share.ts:256-258 — pageHtml length checked in chars not bytes, bypass possible | enforce byte-length limit server-side

SECURITY_TRIAGE: medium — stored XSS via unsanitized pageHtml