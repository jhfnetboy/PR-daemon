FILES:
- apps/api/src/chat.ts — moved render_intent to top-level, added brief_patch fallback
- apps/api/src/render-intent.test.ts — added contract and fallback tests

FINDINGS:
1. [Medium] apps/api/src/chat.ts:1004 — `bp.render_intent` read but `sanitizePatch` drops it from output | ensure fallback also writes to output patch

TRIAGE: significant — core logic change to render intent handling

SKELETON:
The fallback reads `bp.render_intent` but the returned `brief_patch` is sanitized without it, so the signal is still lost downstream. Consider merging the fallback value into the sanitized patch output.