No findings.

Verified from the local checkout that the root `LICENSE` was already Apache 2.0, this PR adds the bilingual compliance files in-tree, updates the README license sections to point at those files, and changes only the project-owned Solidity SPDX headers from `MIT` to `Apache-2.0`. The new `cla.yml` workflow is syntactically valid and does not execute PR-controlled code.

Conclusion: APPROVE
