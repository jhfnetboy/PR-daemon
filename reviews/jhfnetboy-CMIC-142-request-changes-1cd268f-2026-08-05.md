## Verdict: REQUEST_CHANGES (incremental re-review, head `1cd268f`, round 3)

**上一轮 4 条阻断项全部真修了**，我逐条 grep 核实：

| 上轮问题 | 现在 |
|---|---|
| spec 的 `fee_cents` 注释与 tasks 打架 | ✅ `spec.md:239-243` 与 `tasks.md:249,279` 口径一致（费用表本身返回美分，写入方原样落库，只有展示层 ÷100） |
| `spec.md:14` §0 汇总行还叫 `fee_usd` | ✅ 全仓 `fee_usd` 只剩 `progress.md:93` 那条**追述这次修复的变更日志**，属正确用法 |
| `tasks.md` 同一 Task 内自相矛盾 | ✅ 被点名的那两行已一致 |
| 设计稿还说「今天全填 59」 | ✅ 零命中，`:547`/`:626` 都已是 `5900`（美分） |

`progress.md:18` 那条「不在这里重述数字、只指向 tasks.md」的去重声明是这轮最有价值的改动——它治的正是这个 PR 反复栽的那个根因。

**但同一类矛盾第三次复发了，而且这次就夹在本轮改动的两行中间。**

### 🔴 Blocking

1. **[Med] `docs/agent/tasks.md:255` — 单位口径矛盾在 T2.3.0 **内部**，本轮改了它前后两行、独独漏了它。**
   ```
   :249（本轮改）开发范围  ⚠️ 费用表本身就返回美分，写入方原样落库、不做任何乘法
   :255（未改）  验收断言④ 🔴 费用表里的 `59`（美元）落库后必须是 `5900`
   :279（本轮改）T2.3.2   表里直接存美分（5900），换算归属只有一处：表本身
   ```
   `:255` 仍把费用表描述成**存美元**、并要求写入侧换算。照断言 ④ 实现 T2.3.0 的人会写
   `fee_cents = table.get(...) * 100`，而表已经返回 `5900` → **`$5900`**。
   grep 全仓验证：这是**唯一**还把费用表说成「美元」的地方。
   **修法**：`:255` ④ 改成「费用表返回 `5900`（美分），落库值必须**恒等于**该返回值 —— 断言写入路径无任何乘除法」。

2. **[Med] `spec.md:15` / `roadmap.md:52` / `design:326,555,606,607` / `tasks.md:256,309,393` —「把新表加进 `scripts/no-real-pii.sh` 扫描范围」是一条**不可执行**的指令，而且和该脚本自述的设计**正好相反**。**
   我读了 `scripts/no-real-pii.sh` 全文：
   - `:6-17` 用 20 行解释**为什么这个脚本不用黑名单**——「第一版是黑名单，手工列了 4 个客户邮箱域名去 grep……它只能抓到我已经知道的东西，而我需要抓的恰恰是我不知道的东西」（就是那条真客户电话漏网的教训）
   - `:44-51` 扫描范围是 `git ls-files` 全仓，needle 从 `docs/data/` 自动提取
   - 脚本里**没有任何表名或路径清单**（grep `leads|sampling_requests|open_questions` → 零命中）

   三张新表的 schema **本来就已被自动覆盖**。但 `tasks.md:256` 等三处把 `scripts/no-real-pii.sh` 列进了「**涉及文件**」（= 本 Task 要改的文件），照字面开工的人只能去加一份表名黑名单 —— **恰好复活脚本明令否掉的那个模式**。
   更具体地，`tasks.md:393` 断言「脚本没覆盖新表就是空跑 —— 那是最危险的一种绿」，**对这个脚本是假的**，它教了一个与本仓安全网设计相反的心智模型。而 `spec.md:15` 还把这条抬到了 D-7 硬约束的高度。
   **修法**：三处 Task 的「涉及文件」去掉 `no-real-pii.sh`；把那句话统一改写成「本脚本按 `git ls-files` 全仓扫描，新表**无需登记**，验收只需 `./scripts/no-real-pii.sh` 绿」；删掉 `:393` 的空跑警告。**建议一次性全局改写，别逐处打补丁** —— 它出现 6 次以上、跨三份文档。

### 非阻塞（实质性，但不拦）

- **[Low] `tasks.md:308` vs `spec.md:46-48` 与 `tasks.md:315`** — T3.1.1 开发范围说 MATRIX 要让 production 读不到 `leads` 的 email/phone/**address**，但 `spec.md:46-48` 明写「收货地址【不在这里】，见 §4 `sampling_requests.ship_*`」，`leads` 表根本没有 address 列；同 Task 的验收断言（`:315`）也只列 email/phone/website。这是上一轮修掉的「地址落错表」的残留副本，风险是实现者去 `leads` 补一个 address 列。`:308` 的 `address` 应为 `website`。
- **[Low] `design/agent-interaction-v2.md:143` vs `:148-152`** — §3.3 分层表的 L2「触发条件」写死「必须四槽齐备」，5 行后正文写「满足任一」+「明确指令 > 所有禁止条件（客户说了 Go ahead 却还没说装什么 → 照出）」。只看汇总表的人会把四槽做成硬闸 —— 而 `tasks.md:77` 正把这条列为 T1.2.2「最容易被实现成禁止，是本 Task 的核心风险」。缓解是 T1.2.2 自己的表述（`:68`）是对的且已进 #144。
- **[Low] `progress.md:27` vs `:37`** — 分支表说 `preview`「与 main 一致」，同文件 `:37` 说 #143 已合入 `preview`。实测 `git log origin/main..origin/preview` = 1 commit（正是 `65c6927`）。本轮改了紧邻下方的 PR 列表却没动这一行；另外分支表未列 #144/#145/#146 三个特性分支。
- **[Low] `tasks.md:96` vs `:100-101`** — T1.3.1 的「明示话术是配套义务」有开发范围、无验收断言，四条断言全是 `resolveSlot` 的取值行为，`:109` 的证据自述「不动 chat.ts」。T1.5.1（`:163`）在 prompt 层覆盖了「必须明示」，所以不是孤儿；但 T1.3.1 若按现有验收 APPROVE→DONE，这半边就无人认领。建议要么挪进 T1.5.1 并在 `:96` 注明，要么补一条断言。

### 精度类（明确不拦，按纯文档 PR 的口径只作提示）

`progress.md:5` 最后更新日期落后一天；`:57`「下一步」表仍把已 DONE 的 T1.1.1 列第 1（本轮给「Task 数量」加了去重声明，漏了这张同样重述 tasks.md 的表）；`:93` 变更日志乱序；`roadmap.md:89`「当前聚焦 F1.1」已过期；`:77` 依赖图里的 `F3.1.2` 按本文档自述编号法应为 `T3.1.2`；`design:184` 的 `admin/index.ts:36` 实际在 `:38`；T1.3.1/T2.1.2/T2.3.2 的验收命令各引用了一个未列进「涉及文件」的测试文件。

### 核实为**没有问题**的（避免下一轮重复提）

- `design:670`「就写为 69 美元吧」**不是矛盾** —— `:667` 已有显式优先级声明「不一致时以 §8 决策表为准（例：400 元 → 400 美元 → 69 美元 → **59 美元**）」，附录是逐字存档且标明作废。
- **各 Task 引用的代码行号全部准确** —— 逐条比对了 head `1cd268f` 的真实文件：`schema.sql:37`、`web/chat.ts:468/489/107/86-90/510/181/220-240/451/30-34`、`api/chat.ts:159-161/37`、`sample.html:98-104/79/101`、`admin/index.ts:33` 全对。`render-gate.ts`/`knowledge.ts`/`sampling-fee.ts` 缺失属正常（均标 `(新)`，对应 PR 未并入本分支）。
- **台账自洽** —— 24 Task / 14 Feature，DONE 1 + PR_OPEN 3 + READY 4 + BACKLOG 16 = 24，与状态汇总逐格相符；跨里程碑依赖链 T3.1.1→T3.3.1→T2.3.0→{T2.2.1,T2.3.1} 在 `tasks.md:454`、`roadmap.md:77`、`spec.md §6` 三处一致。

### 一条方法建议

三轮阻断项全是同一个形状：**同一个事实写在 N 处，只改了 N-1 处**。单位口径这一条在四份文档里有 8 处独立表述，本轮修 `:249`/`:279` 时又漏了夹在中间的 `:255`。

`progress.md:18` 你自己发明的解法是对的（「不在这里重述数字，指向 tasks.md」），建议推广：**`spec.md` §4 设为单位口径的唯一权威，`tasks.md` 的验收断言改成引用而非复述**（「断言落库值恒等于费用表返回值，口径见 spec.md §4」）。复述归零，这个 class 才会真正结束。

同理，`design:667` 那句「不一致时以 §8 决策表为准」是有效机制——它把 69/59 这个本会成为阻断项的冲突降级成了无害的历史存档。给 `spec.md`（数据口径权威）和 `tasks.md`（执行权威）各加一句同样的声明，比继续逐处同步省事得多。

### Assumptions

- 在独立 worktree（head `1cd268f`）里 grep/读全部 `docs/agent/*.md` 与 `docs/design/*.md`，并对照真实的 `scripts/no-real-pii.sh` 与被引用的源码行号，未改动 `~/Dev/jhfnetboy/CMIC` 任何文件。
- 按纯文档 PR 的口径，只对**实质性错误**（会让实现者写出错代码、悬空引用、与仓库自身状态矛盾）判阻断；内部精度类一律降级为提示。上面两条阻断项都属于前者。
- **R3(Codex PK) 未跑** —— 本会话内它已两次零输出挂起被杀。**R4 未跑** —— R2 独立判定 2-round，
  且两条阻断项都有我自己 grep + 读源码的直接证据。本 review 标 **2 轮 + 机械验证**，不冒充 4 轮。

---
*Reviewed by clestons (`$pr` v4, **2-round + 机械验证**, incremental `c0a6d13`→`1cd268f`): DeepSeek
R1a+R1b（`deepseek-v4-flash`;方向沾边但没定位到 `:255`）→ Sonnet 机械验证（逐条 grep 核实上轮 4 条
阻断项、读 `no-real-pii.sh` 全文确认它没有可登记的清单、`git log origin/main..origin/preview` 核对分支
状态）→ Opus R2（独立评审，确认 `:255` 且指出矛盾在同一 Task 内部、挖出 `no-real-pii.sh` 的错误前提
跨三份文档出现 6 次以上、address 残留、L2 触发条件表与正文相反）→ **Codex R3 挂起未跑；R4 未跑**。*
