.env
.env.*
!.env.example

__pycache__/
*.pyc
.state/
*.sqlite-shm
*.sqlite-wal
deepseek_v3_tokenizer/*

# Playwright MCP 的页面快照 / 控制台日志。**绝不能进版本库** ——
# 它们是浏览器对页面的原样抓取，一旦抓的是登录态页面，里面就会有活凭证
# （一次性链接、会话状态）。CoLivingOS 2026-08-25 因此误提交过 27 个文件、
# 其中四条是真实邀请链接。这个目录在此之前从没人加过 ignore，
# 因为在那之前没人用它抓过后台页面 —— 也就是说，**风险是随用法变的，不是随目录变的**。
.playwright-mcp/
